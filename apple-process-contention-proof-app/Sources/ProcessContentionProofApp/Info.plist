<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!--
	  Deliberately no UIApplicationSceneManifest key: this app uses the
	  legacy, scene-less UIKit application lifecycle (window setup happens
	  directly in AppDelegate.application(_:didFinishLaunchingWithOptions:),
	  with no SceneDelegate) -- the same pattern
	  apple-process-termination-proof-app already established. This one
	  Info.plist and one AppDelegate.swift are shared by all FOUR app targets
	  in this project (ProcessContentionProofAppA/B/C/D); only
	  PRODUCT_BUNDLE_IDENTIFIER differs per target's own build settings, and
	  AppDelegate.swift reads its role/domain from the DATALOOM_ROLE/
	  DATALOOM_DOMAIN environment variables each target is launched with (see
	  .github/workflows/apple-validation.yml) rather than from any
	  source-level or Info.plist-level difference.
	-->
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
</dict>
</plist>
