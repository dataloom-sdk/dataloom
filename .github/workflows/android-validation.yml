name: Android Validation

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android-validate:
    name: Validate Android modules
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      DATALOOM_ANDROID_BUILD: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-provider: basic

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Verify Gradle version
        run: ./gradlew --version

      - name: Reject accidentally committed Gradle plugin metadata
        shell: bash
        run: |
          if [[ -d META-INF/gradle-plugins ]]; then
            echo "Generated META-INF/gradle-plugins must not be committed."
            find META-INF/gradle-plugins -type f -print
            exit 1
          fi

      - name: Verify project structure includes Android modules
        shell: bash
        run: |
          set -o pipefail
          ./gradlew projects \
            --no-configuration-cache \
            --stacktrace \
            --warning-mode all \
            --console=plain 2>&1 | tee android-projects.log

      - name: Upload Android project configuration log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: android-projects-${{ github.run_id }}-${{ github.run_attempt }}
          path: android-projects.log
          if-no-files-found: error
          retention-days: 7

      - name: Assemble dataloom-connectivity-android
        run: ./gradlew :dataloom-connectivity-android:assembleDebug :dataloom-connectivity-android:assembleRelease

      - name: Run dataloom-connectivity-android tests
        run: ./gradlew :dataloom-connectivity-android:testDebugUnitTest

      - name: Lint dataloom-connectivity-android
        run: ./gradlew :dataloom-connectivity-android:lintDebug

      - name: Assemble dataloom-scheduler-workmanager
        run: ./gradlew :dataloom-scheduler-workmanager:assembleDebug :dataloom-scheduler-workmanager:assembleRelease

      - name: Run dataloom-scheduler-workmanager tests
        run: ./gradlew :dataloom-scheduler-workmanager:testDebugUnitTest

      - name: Lint dataloom-scheduler-workmanager
        run: ./gradlew :dataloom-scheduler-workmanager:lintDebug

      - name: Assemble dataloom-queue-room
        run: ./gradlew :dataloom-queue-room:assembleDebug :dataloom-queue-room:assembleRelease

      - name: Run dataloom-queue-room tests
        run: ./gradlew :dataloom-queue-room:testDebugUnitTest

      - name: Lint dataloom-queue-room
        run: ./gradlew :dataloom-queue-room:lintDebug

      - name: Verify committed Room schema is current
        shell: bash
        run: |
          test -f dataloom-queue-room/schemas/io.dataloom.queue.room.internal.DataLoomRoomDatabase/1.json
          git diff --exit-code -- dataloom-queue-room/schemas

      - name: Build all Android modules
        run: ./gradlew :dataloom-connectivity-android:build :dataloom-scheduler-workmanager:build :dataloom-queue-room:build

      - name: Verify KMP modules unaffected
        run: ./gradlew :dataloom-api:allTests :dataloom-core:allTests :dataloom-runtime:allTests :dataloom-testing:allTests
