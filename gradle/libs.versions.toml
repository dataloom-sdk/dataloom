[versions]
kotlin = "2.4.10"
# Kotlin 2.4 class files require AGP 9.1.0 or newer. Keep one AGP version
# across the root build and all Android integration modules.
agp = "9.1.0"
android-compileSdk = "35"
android-minSdk = "21"
android-targetSdk = "35"
kotlinx-coroutines = "1.10.2"
workmanager = "2.10.1"
room = "2.7.1"
# KSP 2.3.1+ supports AGP 9 built-in Kotlin. Use the current stable KSP2 line.
ksp = "2.3.9"
mockito-kotlin = "5.4.0"

[libraries]
# Kotlin Gradle plugin dependency used by build-logic convention plugins
kotlin-gradlePlugin = { group = "org.jetbrains.kotlin", name = "kotlin-gradle-plugin", version.ref = "kotlin" }
# Android Gradle plugin dependency retained for explicit build-tool inspection
android-gradlePlugin = { group = "com.android.tools.build", name = "gradle", version.ref = "agp" }
# Kotlin coroutines for Android
kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "kotlinx-coroutines" }
# WorkManager
workmanager-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "workmanager" }
# Room
room-runtime = { group = "androidx.room", name = "room-runtime", version.ref = "room" }
room-ktx = { group = "androidx.room", name = "room-ktx", version.ref = "room" }
room-compiler = { group = "androidx.room", name = "room-compiler", version.ref = "room" }
room-testing = { group = "androidx.room", name = "room-testing", version.ref = "room" }
# Test utilities
mockito-kotlin = { group = "org.mockito.kotlin", name = "mockito-kotlin", version.ref = "mockito-kotlin" }

[plugins]
kotlin-multiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
android-library = { id = "com.android.library", version.ref = "agp" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
