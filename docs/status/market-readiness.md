# Market-readiness dashboard (full detail)

This is the complete version of the dashboard summarized on the
[repository front page](../../README.md#market-readiness-dashboard). It
carries every dated entry and the full per-gate evidence text; the README
keeps only the current verdict and a short list of recent highlights so the
front page stays readable.

- **Last reconciled:** 2026-08-18
- **Recorded V1 target:** 2026-08-27
- **Current verdict:** **NO-GO — not production-ready or market-ready**
- **Accepted engineering/release gates:** **0 of 10** (`#93`–`#102` all open)

> [!NOTE]
> An earlier version of this dashboard reported `1 of 10` with `#93` marked
> complete. That was inaccurate: `#93` is still missing versioned
> configuration snapshots, a shared policy engine, SDK-wide adoption of the
> secure-random/integrity/HMAC primitives at real key/nonce/token/tamper-evidence
> sites, and an explicit KMP Android target, among other items below.
> Corrected here rather than left standing — see the acceptance rule this
> project holds itself to immediately below.

Progress below is acceptance-based, not a percentage of code written. A gate is
`COMPLETE` only when its issue criteria have executable evidence on the same
reviewed commit. `IN PROGRESS` therefore includes substantial implementations
that still have unqualified release behavior. A gate can absorb real, tested,
merged work for a long time before it flips to `COMPLETE` — that is by design,
not a sign nothing is happening. This log exists so that work is visible in
between gate flips.

The full V1 gate table below also carries an **Est. completion** column. It is
a rough, human-judged estimate of how much of each gate's required capability
set exists on `main` today — derived by weighing that row's own "Finished on
`main`" text against its "Still pending" text, banded to the nearest 5%. It
does **not** replace the `Status` column above it, is not derived from a
checkbox or code-coverage count (every acceptance-criteria checkbox on every
`#93`–`#100` issue is still literally unchecked in the GitHub issue body,
regardless of real progress — those checkboxes are not kept in sync), and a
gate can sit at a high estimate for a long time before its `Status` actually
flips to `COMPLETE`, same as everything else on this page. Treat it as a
progress signal, not an acceptance determination.

## Recently shipped (newest first)

| Date | What landed | Gate/track |
|---|---|---|
| 2026-08-25 | `dataLoomHealthSnapshot` (`#96`) — a bounded first slice of DL-042's "health/read model" gap, investigated rather than assumed missing: searched for an existing `Health`/`HealthSnapshot` type first and found `ProviderHealth`/`ProviderHealthStatus` (per-provider, `dataloom-provider-api`, suspend/async) and `RetryCircuitTelemetrySnapshot` (already a redacted, synchronous read model for retry/circuit scope only, `docs/api/retry-circuit-telemetry.md`) — neither is the SDK-wide aggregation the gate's own text names, so a genuine gap remained. New `dataLoomHealthSnapshot(...)` (`io.dataloom.runtime.observation.health`, `dataloom-runtime`) is a pure, synchronous function — no I/O, no suspension, calls no provider or collaborator itself — that aggregates whichever already-queryable in-process subsystem state a caller supplies into one `DataLoomHealthSnapshot`: `ProviderLifecycleCoordinator.state` (a synchronous property), `BoundedRetryCircuitTelemetry.snapshot()` (already redacted), and caller-awaited `DataLoomProvider.health()` results keyed by `ProviderId`. Investigated whether durable-outbox (`DurableOperationalEventOutbox.entries`) and queue-worker (`QueueWorkerCoordinator`) state belong in this slice too and found neither exposes a synchronous, non-suspending read path today — including them would mean inventing new query capability on subsystems that don't have one yet, so both are named as explicitly out of scope rather than silently omitted. Provider-health redaction reuses `StrictDataLoomRedactor`/`ClassifiedData` via the exact code/category/severity/recoverability-are-`PUBLIC`, message-is-`CONFIDENTIAL` (removed outright by the default policy, not merely masked), cause-never-included convention `SynchronizationOperationalEventBridge` already established for every other `DataLoomError` this SDK exports — proven by a real test that a sensitive error message never survives redaction. `ProviderHealth.details` (free-form `DataLoomMetadata`) is deliberately reported only as a field count, never field-by-field, since no existing conversion from `DataLoomMetadata`'s only-non-blank-required keys into `ClassifiedData`'s bounded ASCII tokens exists to reuse, and inventing one is a separate, larger concern. 8 new tests (`DataLoomHealthSnapshotTest`) covering real state reflection, genuine redaction, and well-defined no-collaborators/empty-section behavior (never a crash), purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`). See `docs/api/health-snapshot.md` | `#96` feature |
| 2026-08-25 | AC-FUNC-004 provider-flow proofs for native Android and KMP iOS (`#94`, `#369`) — investigated `#94`'s "complete AC-FUNC-004 provider flow through native Android, KMP Android, and KMP iOS" pending clause and found it a genuine, bounded, achievable gap distinct from the already-investigated, still-blocked Apple process-termination half (`docs/apple/process-termination-investigation.md`): the existing common/Android-Room/Apple-file AC-FUNC-004 qualification checkpoints each drive `CircuitBreakerExecutionGate`/`CircuitBreakerTransportOperationAdapter` directly, never through `DataLoomBuilder` or a real composed provider stack — both platform checkpoints' own "Remaining acceptance work" sections already named this exact gap. New `AndroidReferenceConsumerRetryCircuitQualificationInstrumentedTest` and `IosReferenceConsumerRetryCircuitQualificationTest` drive the identical deterministic AC-FUNC-004 scenario (backoff + jitter, circuit opens, attempts rejected, half-open probe, recovery) through a real `DataLoomBuilder.providerProtectionConfiguration` wired to the real `RoomCircuitBreakerStateStore`/`AppleFileCircuitBreakerStateStore`, composed with each platform's real provider wiring, driven by repeated real `DataLoom.protectedSynchronization.synchronize()` calls. A genuine finding along the way: the Android test was first attempted as a Robolectric unit test and found to genuinely fail — `RoomCircuitBreakerStateStore`'s `circuit_breaker_states` table does not work under Robolectric's SQLite shadow in this repository's current dependency versions (reproduced in isolation), even though `RoomQueueProvider`/`RoomStorageProvider` (same `DataLoomRoomDatabase`, different tables) do — so it now lives as `androidTest`, matching every other `RoomCircuitBreakerStateStore`-touching test in this repository. KMP Android explicitly not addressed: an explicit KMP-aware Android target remains confirmed blocked (`docs/android/kmp-android-target-blocker.md`), the same category-error shape already found for `#93` this session | `#94` investigation |
| 2026-08-25 | `DurableOperationalEventOutboxProcessor.process` gains bounded, opt-in filtering (`#96`), closing this gate's previously-named "filtering" gap: a new optional `filter: OperationalEventOutboxEntryFilter` parameter (`matches(envelope): Boolean`, the same general-predicate `fun interface` shape `handler` already has) is evaluated over every currently-retained entry for the scope before `maxEntries` is applied, so a rejected entry never counts against the bound and stays retained, untouched, for a later pass — a caller filtering for a rare `OperationalEventType` is never starved by a batch of common non-matching entries. Investigated the outbox-layer alternative (a predicate on `DurableOperationalEventOutbox.entries` itself) and rejected it: that class already loads a scope's entire retained list as one persisted document per call, so filtering earlier saves no read work, only relocates the same in-memory filter into a class whose own documentation already scopes filtering out as separate follow-up work. Default filter accepts every entry, so an unconfigured call is byte-for-byte unchanged; new `OperationalEventOutboxProcessingSummary.filteredOut` counter (always `0` when unconfigured) keeps the summary honest, and every retained entry being rejected returns `Processed` with an all-zero summary rather than `NoWork`, keeping "scope genuinely empty" distinguishable from "filter matched nothing this cycle." 4 new tests, purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`). See `docs/api/operational-envelope-redaction.md`'s "Read-then-consume processing" section | `#96` feature |
| 2026-08-24 | `DurableAssetManifestHistory` (`#97`) — a bounded second slice of `#97`'s (DL-043) versioned-manifest requirement: durable, monotonic, bounded-retention history of an asset's applied `AssetManifest` revisions, the fifth real `DurableStateStore` domain adoption (`io.dataloom.api.asset`, `dataloom-api`), reusing `RoomDurableStateStore` with zero new Room code — following gate `#93`'s own `DurableConfigurationHistory` adoption precedent exactly. Investigated two open design questions before implementing rather than assuming: whether monotonicity should be enforced at this layer even though `AssetManifest` itself deliberately isn't (yes — `apply` rejects a non-strictly-increasing `version`, the same permissive-value-type/disciplined-durable-log split `ConfigurationSnapshot`/`DurableConfigurationHistory` already establish), and whether true multi-revision history needs one scope key per `(AssetId, version)` pair or a single scope key per `AssetId` holding a bounded list of retained revisions in one CAS-written value (chose the latter, the same shape `ConfigurationHistoryState`/`OperationalEventOutboxState` already use, since the former loses "what is current" queryability without a separate index). `TScope` is `AssetId` reused directly (no new wrapper type, matching `DurableUnresolvedConflictLog`'s reuse of `ConflictId`); a new `AssetIdMismatch` outcome rejects a manifest applied under the wrong scope, a check no other `DurableStateStore` domain needed since no other domain's state type carries its own identity field; `AssetManifestHistoryStateCodec` cannot recompute a checksum from decoded fields the way `ConfigurationHistoryStateCodec` does (the checksum is over external asset bytes this codec never sees), so it instead relies on `AssetManifest`/`AssetChunkLayout`'s own constructor validation to fail closed on corrupt decoded data. Deliberately does not add `rollbackToLastKnownGood` (no precedent or identified caller need) or wire a real caller (nothing yet constructs real `AssetManifest` revisions, the same "primitive, not pipeline" posture `AssetManifest` itself shipped with). 33 new tests (17 in `DurableAssetManifestHistoryTest`, 13 in `AssetManifestHistoryStateCodecTest`, 3 in a new Room integration test with a real restart-recovery proof mirroring `DurableUnresolvedConflictLogTest`'s), purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`). See `docs/api/durable-asset-manifest-history.md` | `#97` feature |
| 2026-08-24 | Artifact graph/BOM gap analysis (`#93`), investigating this gate's own "final published artifact graph/BOM... ~20 separate enforced modules" line rather than attempting it directly. `docs/architecture/artifact-graph-bom-gap-analysis.md` builds a precise module-by-module table against `ADR-0002`'s 12 published coordinates and 26 source modules: zero of the 12 published coordinates are publication-ready today (no module anywhere in the repo has any `maven-publish`/`publishing {}` wiring — confirmed by a repository-wide search); several ADR-0002 source modules (`dataloom-events`, `dataloom-state`, `dataloom-queue`, `dataloom-retry`, `dataloom-policy`, `dataloom-conflict`, `dataloom-storage-spi`, `dataloom-transport-spi`, `dataloom-observability`) already exist and are tested but remain co-mingled inside `dataloom-api`/`dataloom-runtime` rather than split into their own source modules; and `dataloom-assets`, `dataloom-jvm`, `dataloom-platform-jvm`, and `dataloom-bom` don't exist at all yet. Investigated whether even the narrowest possible slice — wiring one already-stable module's publication metadata, no source changes, no release-timing decision — was safely bounded, per the task's own suggested escape hatch, and found it is not: `ADR-0002` itself states the `io.dataloom` group is blocked pending `DL-046`'s (`#100`, itself `BLOCKED / NO-GO`) namespace-ownership/release-authority verification, `README.md`'s license status is still "to be finalized before V1 publication," and no version, target artifact repository, or signing keys are configured anywhere in the build — five real business/governance decisions, all confirmed unresolved, not engineering work left to do. No code changed; percentage unchanged (87%) per this session's own scoping-document precedent | `#93` investigation |
| 2026-08-24 | `PolicyEvaluator`/`DurablePolicyDecisionLog`'s first real caller (`#93`), closing the policy half of this gate's named gap ("neither `DataLoomConfigurationResolver.resolve` nor `PolicyEvaluator.evaluate` has a real caller anywhere in `dataloom-runtime` yet"). Investigated the plausible candidates named for this gap first — retry/circuit administration authorization, strategy admission, provider resolution gating, conflict-resolution authorization — before wiring anything: retry/circuit administration already has its own dedicated, mandatory `RetryAdministrationAuthorizer`/`CircuitAdministrationAuthorizer` collaborator per command, and `PolicyDecisionScope` is keyed by `ExecutionId` (the same identifier `ExecutionContext` already carries), which those administrative commands don't carry at all — forcing policy evaluation there would have meant duplicating an already-solved authorization boundary or synthesizing a fake execution identity, not a genuine need. `StrategySynchronizationExecutionCoordinator` was the genuine fit instead: its own class doc already states it "admits a strategy request before resolving or invoking any provider," and every request already carries a real `ExecutionContext` via `StrategySynchronizationRequest.request.context`. New opt-in `DataLoomStrategyAdmissionPolicySpec`/`DataLoomBuilder.strategyAdmissionPolicyConfiguration(spec)`: when configured, the coordinator evaluates the supplied `PolicySet` via `PolicyEvaluator.evaluate` against that real `ExecutionContext` before resolving or invoking any provider, admitting only a `PolicyCheckOutcome.Allow` outcome and rejecting every other outcome kind (`Deny`/`RequireUserAction`/`Defer`, deliberately collapsed for this first bounded slice) with a new `StrategyExecutionRejectionReason.POLICY_DENIED`; when a `decisionLogStore` is also supplied, the resulting `PolicyDecision` is durably committed via `DurablePolicyDecisionLog` under a `PolicyDecisionScope` keyed by the policy set id and the request's own `executionId` — after that decision has already determined admission, never blocking or altering it, with a recording failure swallowed exactly like this coordinator's existing operational-event bridges. `PolicyEvaluationInput.configurationSnapshot` is a fixed value supplied at spec-construction time rather than resolved live per request, since no live per-request `ConfigurationSnapshot` resolution caller exists anywhere in `dataloom-runtime` yet either (`DataLoomConfigurationResolver.resolve` remains the other, still-open half of this gap's named gap). When the spec is not configured, behavior is byte-for-byte unchanged — no policy is ever evaluated. 4 new tests (`DataLoomBuilderStrategyAdmissionPolicyTest`: allowed-and-recorded, denied-and-recorded, denied-with-no-log-configured, and unconfigured-is-unchanged), purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`) | `#93` foundations |
| 2026-08-24 | `AssetManifest` (`#97`) — a bounded first slice of `#97`'s (DL-043) `FR-ASSET-001` versioned asset manifest, this gate's first real work of any kind. A pure, deeply immutable value type (`io.dataloom.api.asset`, `dataloom-api`) describing an already-decided asset shape: identity (`AssetId`)/version, size, media type, whole-object checksum, and chunk geometry, plus optional compression/encryption metadata — no upload/download, chunking execution, streaming, sessions, quotas, cancellation, or content-policy logic. Investigated whether each field needs a real product decision first rather than assuming: whole-object and per-chunk checksums reuse `#234`'s `DataLoomDigest` generically (no new decision needed); chunk geometry is described as an ordered list of contiguous byte ranges (`AssetChunkLayout`/`AssetChunkDescriptor`) rather than a single chunk-size/count pair, deliberately not presupposing fixed-size vs. content-defined chunking; compression/encryption algorithms are open, bounded-token labels (`AssetCompressionAlgorithm`/`AssetEncryptionAlgorithm`) mirroring `OperationalPayloadEncoding`'s "name it without deciding the closed set up front" shape, since which algorithms V1 actually implements remains a genuinely open product decision this slice does not make; `AssetEncryptionMetadata` reuses `KeyReference` exactly as-is, never resolving it to key bytes. 26 new tests (25 in the new `AssetManifestTest`, plus one new `AssetId` case added to the existing shared `IdentifierContractsTest`), purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`). See `docs/api/asset-manifest.md` | `#97` feature |
| 2026-08-17 | `ProviderProtectionStorageBridge` now forwards `readLocalConflictCandidate` (`#322`), closing the last named gap in circuit-breaker-protected conflict detection: `TimeoutEnforcingStorageProvider` already forwarded this call correctly, but the circuit-protection bridge always fell through to the interface's default `NotFound`, regardless of what the wrapped provider actually supported. Fixed by adding a required `StorageCircuitScopes.readLocalConflictCandidate` scope (a genuinely breaking constructor addition — investigated and rejected a default value as unsafe, since it would silently violate the class's own "no scope is inherited or inferred" invariant) and wiring it through `CircuitBreakerStorageOperationAdapter`/`ProtectedStorageOperations` the same way as the other 7 storage operations. The same fix applies to `ProviderProtectionStrategyFallbackBridge`/`ProviderProtectionStrategyReconciliationBridge`/`ProviderProtectionStrategyFallbackAndReconciliationBridge`, which wrap the storage bridge and would otherwise have silently picked up the interface default instead of real forwarding. All 4 real `StorageCircuitScopes` construction sites (test-only; no non-test site exists) updated; new tests prove a tripped circuit rejects the call like every other protected operation and a closed circuit forwards a real `Found` result unchanged | `#93`/`#95` foundations |
| 2026-08-17 | Durable strategy-decision diagnostics — the fourth real `DurableStateStore` domain adoption, closing `#102`'s named "durable strategy events/diagnostics" pending item. New `StrategyDecisionEvent`/`StrategyDecisionEventCodec`/`DurableStrategyDecisionEventLog` (`io.dataloom.api.strategy`) reuse `RoomDurableStateStore` with zero new Room code, same hex-encoded/bounded/fail-closed codec discipline as the other three domains. Deliberately distinct from the strategy engine's existing durable *continuation* state (accepted-plan persistence for durable-queue replay, unchanged) — this is a payload-free audit trail for operator visibility, never read back by any execution path. Wired to a real caller from day one, not shipped as an inert primitive: `StrategySynchronizationExecutionCoordinator`'s `execute` now durably records a `StrategyDecisionEvent` for every terminal result — including early admission rejections — whenever an application opts in via new `DataLoomBuilder.strategyDiagnosticsConfiguration(DataLoomStrategyDiagnosticsSpec)`; unconfigured behavior is byte-for-byte unchanged. One deliberate, documented design deviation from this codebase's other commit-once durable logs: unlike a policy decision or an unresolved conflict, a strategy execution's terminal outcome can legitimately differ across retries of the same `StrategyDecisionId` even though the evaluated plan is deterministic, so a same-scope outcome mismatch is reported as `Conflict` (never silently overwritten) without necessarily meaning a caller bug — see the class's own "Why outcome mismatches are not treated as a caller bug" documentation. 22 new tests (log, codec, Room integration, and a coordinator-level test proving real end-to-end recording through `DataLoomBuilder`), purely additive ABI change (JVM + iOS klib) | `#102` feature |
| 2026-08-17 | `MessageContentRedactor` (`#317`) — a defense-in-depth free-text redaction primitive (`io.dataloom.api.security`), complementary to the existing `DataLoomRedactor`/`ClassifiedData` structured-field redaction. Investigated the actual remaining gap first rather than assuming: the concrete, previously-cited violation (`ApolloErrorMapper` forwarding a wrapped exception's message after only truncating it) was already fixed by `#251`; what genuinely remained was structural — "no type-level guarantee against a future implementation" doing the same. Reference implementation `PatternBasedMessageContentRedactor` deterministically masks common secret-shaped patterns in free text (Bearer tokens, JWT-shaped tokens, AWS access keys, sensitive query-parameter values, URL Basic-Auth credentials, email addresses), explicitly documented as not a general-purpose scanner. Deliberately not wired into a real call site yet — no concrete consumer exists without inventing one, since the one known violation was already fixed by removing forwarding rather than needing redaction. 13 new tests, purely additive ABI change (pure common Kotlin, no platform split needed) | `#93` foundations |
| 2026-08-16 | `DataStoreStorageProvider` adopts `readLocalConflictCandidate` (`#315`) — the fourth and last reference `StorageProvider` to override this capability, **completing adoption across all four reference providers** (Room, SQLDelight, file-based, DataStore). Simplest of the four: `acknowledgeOutboundChanges` already deletes the event record for both `ACCEPTED` and `REJECTED` alike, so no separate accepted/rejected exclusion logic is needed; like file-based, has no per-entity index, so the same documented linear-scan cost applies. 5 new tests. Verified locally against an established pre-existing-failure baseline (this repo's own documented Windows-only `androidx.datastore` file-move limitation, 8/23 tests) rather than guessing at new failures — 2 of 5 new tests hit the identical known signature, confirmed by comparing JUnit XML failure messages side by side; real CI (Linux) confirmed all green | `#93`/`#95` foundations |
| 2026-08-16 | `FileStorageProvider` adopts `readLocalConflictCandidate` (`#313`) — the third reference `StorageProvider` to override this capability, after Room and SQLDelight. Same outbound-only principle, but with no per-entity index — `outbound.idx` is the only ordering this provider persists, so the implementation scans it in order and keeps the last match, a documented cost consistent with this provider's own reference/low-volume scope. `rejected/` is deliberately not consulted: a `REJECTED` event moves there with no ordering information relative to any other outbound entry, so there is no persisted evidence to say which is more recent — proven, not just asserted, via a dedicated test. Matches SQLDelight in deleting an `ACCEPTED` event rather than retaining it. 5 new tests against a real temp-directory filesystem | `#93`/`#95` foundations |
| 2026-08-16 | `SqlDelightStorageProvider` adopts `readLocalConflictCandidate` (`#311`) — the second reference `StorageProvider` to override this capability, after Room, same outbound-only principle. Real platform difference documented and tested rather than reconciled away: this provider's own `acknowledgeOutboundChanges` deletes the row for an `ACCEPTED` event instead of retaining it (Room retains it), so an entity whose only outbound edit was already accepted also correctly reports `NotFound` — a dedicated test proves this for real, not just the KDoc. New `selectLatestOutboundChangeForEntity` SQLDelight query. 4 new tests running against a real SQLite driver (JVM locally, cross-compiled for iOS) | `#93`/`#95` foundations |
| 2026-08-16 | `RoomStorageProvider` adopts `readLocalConflictCandidate` (`#309`) — the first reference `StorageProvider` to override this opt-in-since-`#258` capability. Deliberately considers only the *outbound* change-event log, never inbound: the outbound log is the local application's own record of pending/recent edits, which is exactly what a genuine local-vs-remote conflict compares against — an entity synced only via inbound apply correctly reports `NotFound`, since that's an ordinary remote update with no local edit to disagree with, not a conflict. New `OutboundChangeDao.findLatestEventForEntity` query returns the most recently appended outbound event per entity (change-set insertion order, then in-set event order — the schema has no shared wall-clock ordering across its outbound/inbound tables). 5 new tests (2 mocked-DAO unit, 3 against a real in-memory Room database, including one proving the outbound-only decision for real) | `#93`/`#95` foundations |
| 2026-08-16 | `AppleFileDurableStateStore` (`#307`) — the second real platform implementation of `DurableStateStore` (`#93` foundations), after Room. Reuses `AppleFileCircuitBreakerStateStore`'s proven `flock`/atomic-write discipline exactly; deliberately has no `namespace` concept, unlike `RoomDurableStateStore` — one dedicated file per instance, matching how Apple's other file-based providers (queue, circuit-breaker, retry-administration) already work. Fails closed identically to the Room implementation; `encode`/size checks run before the file lock is taken so a codec/size failure never touches the file. 12 new tests mirror `AppleFileCircuitBreakerStateStoreTest`'s coverage. Verified via cross-compile of production and test sources for all three Apple targets plus the real macOS `apple-validation.yml` CI leg (link + execute); no domain has adopted it yet — the three existing domain adoptions (configuration history, policy decisions, unresolved conflicts) remain Room-only | `#93` foundations |
| 2026-08-14 | Conflict detection wired into `DataLoomBuilder` for real (`#272`) — a foundational gap, not a hybrid-specific one: `InboundPullSynchronizationPipeline` has accepted an optional `InboundPullConflictDetectionConfiguration` since `#258`, and `StorageProvider.readLocalConflictCandidate` has existed since the same change, but nothing in `DataLoomBuilder` ever constructed one. Conflict detection was fully implemented and tested at the pipeline level, yet completely unreachable from any real application, for every direct pipeline and for all six built-in strategies (they share `buildStrategyPipelineRegistry()`). New `DataLoomConflictDetectionSpec` + `DataLoomBuilder.conflictDetectionConfiguration(spec)` close it — opt-in, byte-for-byte unchanged behavior when not called. Proven end-to-end through the public builder API, not just at the pipeline layer: configured detects a real conflict; unconfigured leaves `readLocalConflictCandidate` uncalled. 2 new tests, purely additive ABI change | `#93`/`#95`/`#102` foundations |
| 2026-08-14 | Hybrid's transport-free-PUSH gap closed with a new `AcceptedLocally` result type (`#271`) — `HybridSource.LOCAL` selected for a PUSH-direction request with no `SERVE_LOCAL` and no remote op previously had no result type that could represent it (`HYBRID_LOCAL_PUSH_NOT_YET_SUPPORTED`, a placeholder rejection); `HybridStrategyExecutor` now returns a genuinely transport-free `AcceptedLocally` terminal result — the first result type in the strategy engine that isn't tied to a transport or storage side-effect. The old enum entry is kept (ABI stability) but documented as no longer produced. `HybridStrategyExecutor` now handles every branch the evaluator can produce for `HybridStrategyProfile`. Purely additive ABI change | `#102` feature |
| 2026-08-14 | `dataloom-android` platform artifact (`#270`) — the second slice of `#101` (DL-039A), after the sibling-module-split pattern (proven precedent: `dataloom-storage-sqldelight-android`) was investigated and found not to transfer to `dataloom-model`/`dataloom-api`/`dataloom-core`/`dataloom-runtime` (zero Android-specific code in any of them; a same-shaped sibling would be a hollow, capability-free wrapper). A plain `com.android.library` (not KMP — an explicit KMP Android target remains confirmed blocked, see `docs/android/kmp-android-target-blocker.md`) aggregating the four core Android providers — connectivity, Room storage, Room queue, WorkManager scheduler — with `androidDataLoomProviders(context)`/`installAndroidProviders(...)` convenience wiring. Deliberately excludes DataStore/SQLDelight-android (opt-in individually) and any transport (DataLoom never bundles a default transport). `runtime-android-reference-consumer` refactored to dogfood it instead of hand-wiring the four providers directly. Also caught up `docs/architecture/modules.md` and `docs/android/README.md`, previously missing 5+ real modules | `#101` feature |
| 2026-08-14 | Native Android reference consumer (`#267`) — the first bounded slice of `#101` (DL-039A). New `runtime-android-reference-consumer` module wires `AndroidConnectivityProvider`, `RoomStorageProvider`, `RoomQueueProvider`, and `WorkManagerSchedulerProvider` into one real, buildable `DataLoom` instance through `DataLoomBuilder`'s public API — proving these four already-shipped provider modules actually compose together, which nothing in this repository exercised before. A compile-only fixture, mirroring `runtime-external-consumer`'s own already-established scope: proves compile-time composition, not runtime behavior (no Robolectric/instrumented test infrastructure exists yet — named as a follow-up, not silently claimed) or published-artifact resolution. Found and correctly did not fix a real, unrelated, pre-existing bug along the way: 8 of 23 `DataStoreStorageProviderTest` tests fail on a clean, unmodified `main` (confirmed via `git stash` before attributing it) — flagged as a separate task rather than scope-creeping this PR | `#101` feature |
| 2026-08-13 | Durable queue admission wired for cache-first, offline-first, and hybrid's shared default durable-work branch (`#265`) — the gap named across `#249`/`#259`/`#261` is closed for real, opt-in via a `QueuedSynchronizationWorkEncoder` (the same one `DataLoom.queueSubmission` already uses). New `StrategyDurableQueueAdmitter` bridges the existing, already-correct `StrategyQueueAdmissionEvaluator` (zero callers before this) into a real `QueueProvider.enqueue` call. Also fixes the coordinator's `DEFER`-disposition path, previously a bare in-memory signal regardless of what the plan required — this is offline-first's actual "no connectivity, durably admit local intent, survive process death" case. Offline-first's durable branch (and hybrid's PUSH-direction fallback) carries `ENQUEUE_DURABLE_WORK` alongside a full synchronous remote leg; running both risked a duplicate remote call once the durably admitted continuation is later processed by a queue worker, so admission replaces the synchronous attempt there (new `DurablyEnqueued` result) rather than running both. Cache-first/hybrid's `SERVE_LOCAL`-present branches admit alongside serving local state instead (`ServedFromCache.durableQueueEntryId`). A full-facade coordinator test (not just executor-level tests, which hand-build providers unconditionally) caught a real gap along the way: a plan's synchronous `requiredCapabilities` never include `TRANSPORT` for these durable branches, but the durable continuation does — provider resolution now widens to the continuation's requirements too, but only when an encoder is configured. Fully backward compatible: unconfigured callers see byte-for-byte identical behavior. 26 new tests, purely additive ABI change | `#102` feature |
| 2026-08-12 | Adaptive strategy execution verified end-to-end — no dedicated `AdaptiveStrategyExecutor` needed, by design (`#263`). `BuiltInSynchronizationStrategyEvaluator.evaluateAdaptive` resolves an `AdaptiveStrategyProfile` to one of its concrete candidates and delegates to that candidate's own evaluation branch, so it always reaches one of the five already-real strategy executors. Verifying this end-to-end (rather than assuming it, per this project's own discipline) surfaced a real, already-shipped bug: `RemoteFirstStrategyExecutor` and `HybridStrategyExecutor` both cast `request.profile` to their own concrete profile type unconditionally, which throws `ClassCastException` for any adaptive-resolved request (`request.profile` stays the outer adaptive profile, never replaced with the resolved candidate). Reproduced the real `ClassCastException` before fixing, per standing verify-before-fix discipline. Fixed with a new internal `resolvedProfile()` helper reading the resolved candidate via `effectiveProfileId` — purely internal, zero ABI impact. 5 new tests, full `jvmTest` and repo-wide `checkKotlinAbi` green | `#102` feature |
| 2026-08-12 | `HybridStrategyExecutor` — the fifth real strategy executor, after network-only, remote-first, cache-first, and offline-first (`#261`). The evaluator pre-selects `HybridSource.LOCAL`/`REMOTE` at evaluation time (not reactively, unlike remote-first's runtime fallback), so this executor just runs whichever source was selected, honoring `persistRemoteResult` via the same non-persisting transport-only path remote-first uses. Two branches are explicitly rejected rather than silently misexecuted: the reconciled-fallback branch (shares cache-first/offline-first's `DURABLE_REFRESH_NOT_YET_SUPPORTED` root cause) and a newly-discovered transport-free PUSH plan shape (`LOCAL` selected with no `SERVE_LOCAL` and no remote op) that no existing result type can represent — a new `HYBRID_LOCAL_PUSH_NOT_YET_SUPPORTED` reason instead of inventing a signature-incompatible success value. With all five concrete strategies now dispatched, the coordinator's `UNSUPPORTED_PLAN` fallback branch became unreachable via the public evaluator, so a test that depended on it was removed rather than repointed a third time. 13 new tests, purely additive ABI change (new class is `internal`) | `#102` feature |
| 2026-08-11 | `OfflineFirstStrategyExecutor` — the fourth real strategy executor, after network-only, remote-first, and cache-first (`#259`). Every plan this executor sees always carries a remote leg (the evaluator only reaches `EXECUTE` when connectivity is available); `SERVE_LOCAL` reuses the same `StrategyLocalFallbackProvider` pattern as cache-first, and `RECONCILE` (the profile default) is honored for real via `StrategyReconciliationProvider`, the first live-evaluation caller of that hook. The default durable-queue-admission path (`requireDurableQueue = true`) is explicitly rejected rather than silently misexecuted, reusing cache-first's `DURABLE_REFRESH_NOT_YET_SUPPORTED` reason for the same root-cause gap instead of duplicating it. 10 new tests, purely additive ABI change (new class is `internal`) | `#102` feature |
| 2026-08-11 | `DurableConflictDetectionCoordinator` wired into `InboundPullSynchronizationPipeline` for real, opt-in, best-effort conflict detection during inbound pull — the first `SynchronizationPipeline` to actually run conflict detection. Required a new opt-in `StorageProvider` capability (`readLocalConflictCandidate`, safe-defaulted so every existing provider keeps compiling unchanged) since no existing read gave the pipeline a local `ChangeEvent` to compare against; found and fixed a real decorator gap along the way (`TimeoutEnforcingStorageProvider` wasn't forwarding the new call) and named, rather than silently left, a second one (`ProviderProtectionStorageBridge`, needs a larger circuit-scope change). Detection is strictly observational — never blocks or alters `applyInboundChanges` (`#258`) | `#93`/`#95` foundations |
| 2026-08-11 | `DurableConflictDetectionCoordinator` — the first real runtime caller of a durable-state adapter shipped this cycle. Wraps `SynchronizationConflictOrchestrator.detectAndResolve` and durably records unresolved outcomes via `DurableUnresolvedConflictLog`, without reaching inside the orchestrator or changing its documented storage boundary; a durable-recording failure never hides the real orchestration result. Not yet called by any `SynchronizationPipeline` (`BidirectionalSynchronizationPipeline` never called the orchestrator at all before this) (`#257`) | `#93`/`#95` foundations |
| 2026-08-11 | Third real domain adoption of the durable-state contract — `DurableUnresolvedConflictLog`, a bounded first slice of ADR-0002's "durable unresolved-conflict" requirement: commit-once records of conflicts `SynchronizationConflictOrchestrator` could not auto-resolve (no resolver configured/found), payload-free by construction (never carries the original `ChangeEvent`). Reuses `RoomDurableStateStore` with zero new Room code — three domains in, the generic-store bet keeps paying off (`#256`) | `#93` foundations |
| 2026-08-10 | Second real domain adoption of the durable-state contract — `DurablePolicyDecisionLog` (commit-once durable record of `PolicyDecision`s, keyed by `PolicySetId`+`ExecutionId`; idempotent-retry vs. genuine-conflict distinguished, matching ADR-0002's "the resulting decision/evidence is committed with the state transition"), a reference codec (`PolicyDecisionRecordCodec`), and proof that `RoomDurableStateStore` genuinely generalizes — this domain reuses it directly with zero new Room DAO/entity code, just a codec and a `KeyEncoder` (`#255`) | `#93` foundations |
| 2026-08-10 | First real domain adoption of the durable-state contract — `DurableConfigurationHistory` (durable configuration snapshot history: `DataLoomConfigurationHistory`'s monotonic-version/bounded-retention/rollback semantics, now persisted via `DurableStateStore` with a bounded compare-and-set retry loop instead of held in a process-local field), a checksum-verified reference text codec (`ConfigurationHistoryStateCodec`), and `RoomDurableStateStore<TScope, TState>` — the first platform implementation of the generic contract, reusable by any future domain via injected `DurableStateScopeKeyEncoder`/`DurableStateCodec` rather than one-off persistence code per domain. Policy decisions remain the next unadopted domain (`#254`) | `#93` foundations |
| 2026-08-10 | Domain-agnostic durable-state contract — `DurableStateStore<TScope, TState>`, generalizing `CircuitBreakerStateStore`'s proven atomic load/compare-and-set pattern with schema-version/migration support (`DurableStateMigration`, `applyMigration`). Proven with a real in-memory implementation and 16 tests, not just a paper contract. No domain has adopted it as its real persistence path yet — that's the next step for configuration snapshots and policy decisions, both still in-memory-only (`#253`) | `#93` foundations |
| 2026-08-10 | Security-defaults audit: fixed a live default-`toString()` exposure gap on `DataLoomError.cause` (a plain `data class`'s auto-generated `toString()` renders a wrapped exception's raw message, which this codebase doesn't control) across all 32 confirmed implementations; documented the separate, still-open `message`-content redaction gap rather than silently leaving it unmentioned (`#251`) | `#93` foundations |
| 2026-08-10 | `CacheFirstStrategyExecutor` — the third real strategy executor, after network-only and remote-first (`#249`). Serve-from-cache and synchronous (non-durable) refresh work today; the default durable/scheduled-refresh path is explicitly rejected (`DURABLE_REFRESH_NOT_YET_SUPPORTED`) rather than silently misexecuted, pending separate queue-admission wiring. New public `ServedFromCache` result type, 10 new tests, purely additive ABI change | `#102` feature |
| 2026-08-10 | Direct test coverage for `StrategySynchronizationExecutionCoordinator`'s admission logic — PROVIDERS_NOT_INITIALIZED, STRATEGY_REJECTED, DEFER, INCOMPATIBLE_TRIGGER, INCOMPATIBLE_INPUT, UNSUPPORTED_PLAN, PROVIDER_RESOLUTION_FAILED, 7 tests (`#247`); closes out direct test coverage for the entire network-only/remote-first execution path (`#241`+`#243`+`#245`+`#247`, 34 tests total, zero production code changed) | `#102` hardening |
| 2026-08-10 | Direct unit test coverage for `RemoteFirstStrategyExecutor` — SERVE_LOCAL fast path, transport-only PULL, non-persisting BIDIRECTIONAL, and the provider-backed pipeline branch, 13 tests (`#245`); third hardening slice of the `#101`/`#102` pivot, closing a previously-undocumented zero-coverage gap on the second of only two strategies with real execution logic | `#102` hardening |
| 2026-08-10 | PUSH/BIDIRECTIONAL coordinator-level test coverage for network-only in `DataLoomBuilderProtectedStrategyTest` — the provider-protection/circuit-breaker layer previously only covered PULL (`#243`); second hardening slice of the `#101`/`#102` pivot | `#102` hardening |
| 2026-08-10 | Direct unit test coverage for `NetworkOnlyStrategyExecutor` — PUSH/PULL/BIDIRECTIONAL, all three acknowledgement-validation failure modes (`#241`); first hardening slice of the `#101`/`#102` pivot, closing a previously-undocumented zero-coverage gap on the network-only executor | `#102` hardening |
| 2026-08-09 | Deterministic policy foundation primitives — `PolicyEvaluator`, `PolicySet`, `PolicyCheckOutcome` (allow/deny/require-user-action/defer), time-bounded fail-closed evaluation (`#239`) | `#93` foundations |
| 2026-08-09 | Versioned configuration snapshot/precedence/rollback primitives — `DataLoomConfigurationResolver`, `ConfigurationSnapshot`, `DataLoomConfigurationHistory` (`#236`) | `#93` foundations |
| 2026-08-09 | Integrity and key-reference security primitives — `DataLoomDigestCalculator` (SHA-256/512), `DataLoomHmacCalculator` (HMAC, the "signature" stand-in), and `KeyReference` — for JVM, Android, and iOS (`#234`) | `#93` foundations |
| 2026-08-09 | Reference SQLDelight `StorageProvider` (Android + iOS); Android driver split into `dataloom-storage-sqldelight-android` to resolve an AGP 9 + Kotlin Multiplatform plugin incompatibility (`#216`) | Adoption readiness |
| 2026-08-09 | Reference Ktor `TransportProvider` (Android + iOS + JVM) (`#213`) | Adoption readiness |
| 2026-08-09 | Reference GraphQL `TransportProvider` (Apollo Kotlin, Android + iOS) (`#224`) | Adoption readiness |
| 2026-08-09 | Reference Room-backed `StorageProvider` for opaque outbound/inbound change sets and checkpoints (`#212`) | Adoption readiness |
| 2026-08-09 | Reference Retrofit/OkHttp `TransportProvider` (Android/JVM) (`#227`) | Adoption readiness |
| 2026-08-09 | Reference file-backed `StorageProvider` — zero database dependency, Android + iOS (`#219`) | Adoption readiness |
| 2026-08-09 | Reference gRPC-backed `TransportProvider` (grpc-kotlin, Android/JVM) (`#225`) | Adoption readiness |
| 2026-08-09 | `DataLoomSecureRandom` cryptographic randomness boundary for JVM/Android/iOS (`#230`) | `#93` foundations |
| 2026-08-09 | Corrected the dashboard's stale `1 of 10`/`#93`-complete claim to `0 of 10` | Documentation accuracy |
| 2026-08-08 | Production `DataLoomClock`/`DataLoomMonotonicClock` for JVM, Android, and iOS (`#221`) | `#93` foundations |
| 2026-08-08 | Reference DataStore-backed `StorageProvider` for small key-value/cache-first data (`#220`) | Adoption readiness |
| 2026-08-08 | Compile-verified getting-started quickstart guide (`#214`) | Adoption readiness |
| 2026-08-08 | 7 more reference-provider modules opened and in flight (Room, SQLDelight, file-based, Ktor, Retrofit, GraphQL, gRPC) | Adoption readiness |
| 2026-08-08 | Repository cleanup: 48 stale branches deleted, 10 superseded process issues closed | Repository hygiene |

## Full V1 gate table

| Next priority | V1 gate | Status | Est. completion | Finished on `main` | Still pending before the gate is complete |
|---:|---|---|---:|---|---|
| 0 | [DL-039 foundations, artifacts, compatibility](https://github.com/dataloom-sdk/dataloom/issues/93) | PARTIAL | 87% | Public API/provider/runtime boundaries, module rules, JVM and Kotlin/Native ABI baselines, external-consumer compilation, durable queue/retry/circuit state primitives, canonical operational envelope/redaction and deterministic bounded V1 wire codec/upcast registry, production wall-clock/monotonic-clock implementations for JVM, Android, and iOS (`#221`, 2026-08-08), a `DataLoomSecureRandom` cryptographic randomness boundary for JVM, Android, and iOS (`#230`, 2026-08-09), integrity/key-reference primitives — `DataLoomDigestCalculator`, `DataLoomHmacCalculator`, `KeyReference` — for JVM, Android, and iOS (`#234`, 2026-08-09), a bounded first slice of versioned configuration snapshots/precedence/rollback — `DataLoomConfigurationResolver`, `ConfigurationSnapshot`, `DataLoomConfigurationHistory` (`#236`, 2026-08-09), a bounded first slice of a deterministic policy foundation — `PolicyEvaluator`, `PolicySet`, `PolicyCheckOutcome` (`#239`, 2026-08-09), a security-defaults audit fixing default-`toString()` secret/credential exposure risk across all 32 `DataLoomError` implementations (`#251`, 2026-08-10), a domain-agnostic durable-state contract — `DurableStateStore<TScope, TState>`, generalizing `CircuitBreakerStateStore`'s proven atomic load/compare-and-set pattern with schema-version/migration support (`#253`, 2026-08-10), its first real domain adoption — `DurableConfigurationHistory` for configuration snapshot history, a checksum-verified reference codec, and a production `RoomDurableStateStore<TScope, TState>` platform implementation reusable by any future domain (`#254`, 2026-08-10), a second real domain adoption proving that reusability for real — `DurablePolicyDecisionLog` (commit-once durable `PolicyDecision` records, idempotent-retry vs. genuine-conflict distinguished) reusing `RoomDurableStateStore` with zero new Room code, just a codec and a `KeyEncoder` (`#255`, 2026-08-10), a third — `DurableUnresolvedConflictLog`, a bounded first slice of ADR-0002's "durable unresolved-conflict" requirement, payload-free by construction, again zero new Room code (`#256`, 2026-08-11), that log's first real caller — `DurableConflictDetectionCoordinator`, wrapping `SynchronizationConflictOrchestrator.detectAndResolve` to durably record unresolved outcomes without reaching inside the orchestrator or hiding real results behind durable-recording failures (`#257`, 2026-08-11), and that coordinator wired into `InboundPullSynchronizationPipeline` for real, opt-in conflict detection — the first `SynchronizationPipeline` to actually run it — via a new opt-in, safe-defaulted `StorageProvider.readLocalConflictCandidate` capability (`#258`, 2026-08-11), and that pipeline-level capability finally wired into the public facade — `DataLoomConflictDetectionSpec` + `DataLoomBuilder.conflictDetectionConfiguration(spec)`, closing a gap where conflict detection was fully implemented and tested but completely unreachable from any real application for every pipeline (`#272`, 2026-08-14), and a second real platform implementation of the durable-state contract — `AppleFileDurableStateStore`, reusing `AppleFileCircuitBreakerStateStore`'s proven `flock`/atomic-write discipline, one dedicated file per instance rather than a shared namespace (`#307`, 2026-08-16), and all four reference-provider adoptions of `readLocalConflictCandidate`, now complete — `RoomStorageProvider` (`#309`, 2026-08-16), `SqlDelightStorageProvider` (`#311`, 2026-08-16), `FileStorageProvider` (`#313`, 2026-08-16), and `DataStoreStorageProvider` (`#315`, 2026-08-16), all comparing only against outbound (local-edit) history, never inbound; SQLDelight's accept-deletes-the-row acknowledgement lifecycle, file-based's no-per-entity-index linear scan and `rejected/`-directory exclusion, and DataStore's delete-on-both-accepted-and-rejected lifecycle are each documented and tested as real, deliberate platform differences, not reconciled away, and a defense-in-depth free-text redaction primitive — `MessageContentRedactor`/`PatternBasedMessageContentRedactor`, masking common secret-shaped patterns in unstructured content such as `DataLoomError.message`, complementary to the existing structured-field `DataLoomRedactor` (`#317`, 2026-08-17), and the previously-named `ProviderProtectionStorageBridge` gap now closed — `readLocalConflictCandidate` is genuinely circuit-protected through a new required `StorageCircuitScopes.readLocalConflictCandidate` scope (an intentional, reviewed breaking constructor addition; a default was investigated and rejected as unsafe, see the PR), matching `TimeoutEnforcingStorageProvider`'s existing pass-through forwarding, with the same fix applied to the strategy fallback/reconciliation bridges that wrap it (`#322`, 2026-08-17), and `PolicyEvaluator`/`DurablePolicyDecisionLog`'s own first real caller, investigated rather than forced onto an already-solved authorization boundary — retry/circuit administration already has its own dedicated, mandatory `Authorizer` collaborator, and `PolicyDecisionScope` is keyed by `ExecutionId`, which those administrative commands don't carry, so `StrategySynchronizationExecutionCoordinator`'s own real admission boundary (its class doc already says "admits a strategy request before resolving or invoking any provider") was the genuine fit instead: a new opt-in `DataLoomStrategyAdmissionPolicySpec`/`DataLoomBuilder.strategyAdmissionPolicyConfiguration` evaluates a configured `PolicySet` via `PolicyEvaluator.evaluate` against the request's real `ExecutionContext` before any provider is resolved, admitting only an `Allow` outcome and rejecting every other outcome kind with a new `StrategyExecutionRejectionReason.POLICY_DENIED`, and optionally commits the resulting `PolicyDecision` durably via `DurablePolicyDecisionLog` after it has already determined admission, never blocking or altering that decision; byte-for-byte unchanged when unconfigured (`#93`, 2026-08-24) | Adopting the durable-state contract for events/assets/audit (the remaining out-of-scope domains) and for resolved `ConflictResolutionDecision`s (a separate, larger design question given `Merge`'s payload); wiring `DurableConfigurationHistory` into a real call site — genuinely blocked, not just undone: for `DataLoomConfigurationResolver.resolve`, a targeted investigation (`docs/api/configuration-resolver-caller-investigation.md`, 2026-08-24) confirmed no subsystem in `dataloom-runtime` today produces more than one `ConfigurationSource`-shaped layer for the same setting for it to arbitrate between — every one of the thirteen existing `*Configuration` classes `DataLoomBuilder` accepts is a flat, single-source struct, and nothing in this repository fetches remote configuration or reads a local-override file; the new strategy-admission policy caller is deliberately narrow — only `PolicyCheckOutcome.Allow` admits, with `Deny`/`RequireUserAction`/`Defer` all collapsed into one `POLICY_DENIED` reason and no richer per-outcome result mapping yet, and its `ConfigurationSnapshot` is a fixed value supplied at builder time rather than resolved live per request, since no live per-request resolution caller exists yet either; any real domain adopting `AppleFileDurableStateStore` (the implementation exists and is verified, but every existing domain adoption remains Room-only); a real call site adopting `MessageContentRedactor` (the primitive exists and is tested, but nothing currently needs it — the one known violation was already fixed by removing unsafe forwarding rather than needing redaction); the `RetryPolicy`/`StrategyPolicy`-onto-policy-foundation migration this clause used to name, now investigated and closed as not achievable: `docs/api/retry-strategy-policy-migration-investigation.md`, 2026-08-25 — both `RetryPolicy` (numeric backoff/jitter timing math) and `StrategyPolicy` (an ordered, multi-field `StrategyExecutionPlan` covering operations, origin, consistency, fallback, and durable continuation) make decisions structurally incompatible with `PolicyCheckOutcome`'s four-way graded allow/deny/require-user-action/defer vocabulary, so "migrating" either would be a lossy, duplicative re-encoding rather than a real improvement, and both are heavily-used public API (sixteen `dataloom-runtime` files construct or consume `RetryPolicy` alone) where a breaking redesign would be a large, separately-scoped migration, not a bounded slice — this closes the last third of the clause the digest/HMAC/secure-random investigation (`docs/api/digest-hmac-secure-random-adoption-investigation.md`, 2026-08-24) left named separately; an explicit KMP Android target (confirmed blocked, see `docs/android/kmp-android-target-blocker.md`); and the final published artifact graph/BOM — a precise gap analysis (`docs/architecture/artifact-graph-bom-gap-analysis.md`, 2026-08-24) replaces the previous "~20 separate enforced modules" approximation with an exact module-by-module table against `ADR-0002`'s 12 published coordinates and 26 source modules (zero of the 12 are publication-ready; several ADR-0002 source modules — `dataloom-events`/`dataloom-state`/`dataloom-queue`/`dataloom-retry`/`dataloom-policy`/`dataloom-conflict`/`dataloom-storage-spi`/`dataloom-transport-spi`/`dataloom-observability` — are still co-mingled inside `dataloom-api`/`dataloom-runtime` rather than split into their own modules, and `dataloom-assets`/`dataloom-jvm`/`dataloom-platform-jvm`/`dataloom-bom` don't exist at all yet) and confirms no single-module publication-metadata slice is genuinely bounded yet either: `ADR-0002` itself states the `io.dataloom` group is blocked pending `DL-046`'s namespace-ownership/release-authority verification (`#100`, itself `BLOCKED / NO-GO`), `README.md`'s license status is still "to be finalized," and no version, target repository, or signing keys are configured anywhere in the build — real business/governance decisions, not engineering work still to do |
| 1 | [DL-039B six strategy engine](https://github.com/dataloom-sdk/dataloom/issues/102) | IN PROGRESS | 80% | Versioned contracts and deterministic planner for all six strategies; direct network-only, remote-first, cache-first (bounded first slice — serve-from-cache and synchronous refresh, `#249`, 2026-08-10), offline-first (`#259`, 2026-08-11), hybrid (`#261`, 2026-08-12), and adaptive (`#263`, 2026-08-12) — all six strategies now execute correctly, five with dedicated executors and adaptive verified end-to-end routing through them with no dedicated executor needed by design; that verification also caught and fixed a real `request.profile` unsafe-cast `ClassCastException` bug in the already-shipped `RemoteFirstStrategyExecutor` (would have hit hybrid too); durable queue admission now genuinely wired for cache-first/offline-first/hybrid's shared default durable-work branch and the coordinator's `DEFER` path, opt-in via a configured encoder (`#265`, 2026-08-13); hybrid's transport-free-PUSH branch now returns a genuine `AcceptedLocally` result instead of a placeholder rejection — `HybridStrategyExecutor` handles every branch the evaluator can produce (`#271`, 2026-08-14); conflict detection now reachable from `DataLoomBuilder` for all six strategies' shared inbound-pull leg, closing a gap where it was implemented and tested but unreachable from any real application (`#272`, 2026-08-14); Room v8/Apple v4 accepted-plan persistence; exact encoder/resolver correspondence; direct, protected, and queued replay without current-policy evaluation; typed fallback and reconciliation hooks, the latter now genuinely exercised outside the durable-continuation path by offline-first's `RECONCILE` branch; durable strategy-decision diagnostics — `StrategyDecisionEvent`/`DurableStrategyDecisionEventLog`, wired into `StrategySynchronizationExecutionCoordinator` for real via opt-in `DataLoomBuilder.strategyDiagnosticsConfiguration`, distinct from the existing durable-continuation state and never read back by any execution path (`#321`, 2026-08-17) | This gate's own acceptance criteria require native Android, KMP Android, and KMP iOS to expose equivalent observable decisions and recovery guarantees — blocked on `#101` platform parity, not on strategy-engine logic; the full connectivity/cache/retry/conflict/cancellation/restart decision matrix audited per built-in profile; a per-attempt outcome history for strategy-decision diagnostics (today's log keeps only the first-recorded terminal outcome per decision, a deliberately separate design question) |
| 2 | [DL-039A Android/KMP/iOS parity](https://github.com/dataloom-sdk/dataloom/issues/101) | IN PROGRESS | 70% | Native Android adapters, Kotlin/Native Apple targets, XCFramework assembly, Swift smoke compilation, a native Android reference consumer (`#267`) and an iOS reference consumer (`#299`) each proving their four core providers compose with `DataLoomBuilder`, real `dataloom-android` (`#270`) and `dataloom-platform-ios` (`#275`/`#292`/`#294`) platform aggregation artifacts, genuine runtime evidence on both platforms rather than compile-only proof — Robolectric (`#300`) and Kotlin/Native iOS Simulator (`#301`) `initialize()`/`shutdown()` proofs, a real `DataLoom.synchronize()` PULL pass on both platforms genuinely writing to storage (`#302`, `#303`) — and now a real Gradle Managed Device AVD emulator proof for Android (`#304`), one tier more real than Robolectric, reusing `dataloom-queue-room`'s/`dataloom-storage-room`'s existing managed-device CI mechanism, and a real Android durable-queue-admission-then-replay proof for offline-first — `AndroidReferenceConsumerDurableQueueRobolectricTest` durably admits an `OfflineFirstStrategyProfile` (`requireDurableQueue = true`, connectivity available) plan via `DataLoom.synchronize(StrategySynchronizationRequest)` (a real `DurablyEnqueued` result, not a synchronous execution — zero transport calls at that point), reads the entry back out of a real Room-backed queue, and replays it through one deterministic `DataLoom.queueWorker.run(...)` cycle — the same capability the real `DataLoomCoroutineWorker`/`WorkManager` bridge uses in production — with `AcceptedStrategyPlanExecutionCoordinator` (never exercised by `#302`/`#303`) genuinely applying the inbound pull to real storage, observed via a real `SynchronizationObserver` (`summary.inboundEventsApplied == 1`) (`#325`, 2026-08-18), and now the iOS counterpart proving the identical shape — `IosReferenceConsumerDurableQueueTest`, executed by `iosSimulatorArm64Test`/`iosX64Test` on macOS CI, durably admits the same `OfflineFirstStrategyProfile` slice via `DataLoom.synchronize(StrategySynchronizationRequest)` (a real `DurablyEnqueued` result, zero transport calls at that point) through the real `AppleFileQueueProvider` file-backed queue, reads the entry back out via a genuine `QueueProvider.acquire` call, and replays it through one deterministic `DataLoom.queueWorker.run(...)` cycle — the same `AcceptedStrategyPlanExecutionCoordinator`/`StrategyDurableQueueAdmitter` `commonMain` production logic `#325` proved on Android, confirmed platform-agnostic by inspection — genuinely applying the inbound pull to real storage, observed via a real `SynchronizationObserver` (`summary.inboundEventsApplied == 1`); unlike Android, no Apple counterpart to `dataloom-scheduler-workmanager`'s `DataLoomCoroutineWorker`/`WorkManager` bridge exists in this repository, so this proves the deterministic single-pass capability only, not a real `BGTaskScheduler`-triggered tick (`#334`, 2026-08-22), and now a real Android durable-queue-admission-then-replay proof for a *second* strategy, cache-first — `AndroidReferenceConsumerCacheFirstQueueRobolectricTest` investigated cache-first's documented PULL/BIDIRECTIONAL `SERVE_LOCAL`-refresh durable branch and found it genuinely not exercisable against the real, unmodified `RoomStorageProvider` (which implements only `StorageProvider`, not `StrategyLocalFallbackProvider` — the same real constraint `#325` already documented), then found a second, previously-undocumented `ENQUEUE_DURABLE_WORK` branch reached for `SynchronizationDirection.PUSH` with connectivity unavailable whose durable continuation is exactly `[READ_LOCAL, PUSH_REMOTE]` — `AcceptedStrategyPlanExecutionCoordinator`'s supported PUSH replay shape, with no `SERVE_LOCAL`/`RECONCILE` requirement the real storage provider can't satisfy — and durably admits a `CacheFirstStrategyProfile` (`requireDurableRefresh = true`, the default) plan via `DataLoom.synchronize(StrategySynchronizationRequest)` (a real `Deferred` result with a non-null `queueEntryId`, zero transport calls at that point), reads the entry back out of the real Room-backed queue, and replays it through one deterministic `DataLoom.queueWorker.run(...)` cycle with `AcceptedStrategyPlanExecutionCoordinator` genuinely running the real, registered `OutboundPushSynchronizationPipeline` against real Room storage, observed via a real `SynchronizationObserver`; because `StorageProvider` exposes no public API to seed local pending outbound changes, the genuine terminal result is `SynchronizationResult.Skipped(NO_CHANGES)` — a real, empty-table Room query — rather than an applied/pushed count, a documented (not silently accepted) limitation of the current storage-provider contract (`#337`, 2026-08-23), and now the iOS counterpart for that same cache-first PUSH/DEFER branch — `IosReferenceConsumerCacheFirstQueueTest` confirmed the identical `commonMain` `evaluateCacheFirstPush`/`AcceptedStrategyPlanExecutionCoordinator` constraint holds on iOS too (the real `SqlDelightStorageProvider` also implements only `StorageProvider`, not `StrategyLocalFallbackProvider`/`StrategyReconciliationProvider`, ruling out `SERVE_LOCAL`/`RECONCILE` branches the same way `RoomStorageProvider` did), but found a genuine, real platform difference along the way: unlike Room's `internal` outbound DAO, `SqlDelightStorageProvider` exposes a `public persistOutboundChanges` method, so this proof seeds one real outbound `ChangeEvent` before admission and its terminal result is a genuine `SynchronizationResult.Succeeded` (`outboundEventsAccepted == 1`) after a real push through the test transport and a real deletion of the acknowledged row — one durable-queue-admission-then-replay bar stronger than `#337`'s honest `Skipped(NO_CHANGES)` finding for Android, which remains accurate for Android (`#338`, 2026-08-23), and RoomStorageProvider's own outbound-seeding gap that `#337`/`#338` surfaced is now closed at the storage-provider level: `RoomStorageProvider` exposes a public `persistOutboundChanges(changeSet)` method mirroring `SqlDelightStorageProvider`'s exact shape, delegating to the previously-`internal`-only `OutboundChangeDao.appendChangeSet` (investigation confirmed that DAO method had no other real production writer anywhere in this repository — only test code called it directly, and `dataloom-storage-file`'s `storeOutboundChangeSet`/`dataloom-storage-datastore`'s `enqueueOutboundChanges` already had the equivalent public capability, leaving Room the sole gap of the four reference storage providers), verified by real JVM `RoomStorageProviderTest` unit tests and a real in-memory-Room `RoomStorageProviderInstrumentedTest` case that seeds a change exclusively through the new public API, reads it back via `readOutboundChanges`, and confirms `acknowledgeOutboundChanges` still deletes it afterward; `#337`'s own `AndroidReferenceConsumerCacheFirstQueueRobolectricTest` has since been updated to consume this new capability: it now seeds one real outbound `ChangeEvent` through `RoomStorageProvider.persistOutboundChanges` before admission and genuinely reaches `SynchronizationResult.Succeeded` (`outboundEventsAccepted == 1`, `outboundEventsRead == 1`) after a real push through the test transport and a real deletion of the acknowledged row, closing the last asymmetry `#337`/`#338` left open and matching `#338`'s iOS bar exactly on Android's own real storage provider (`#93` closed the storage-provider capability gap, 2026-08-23; `#342` updated the reference-consumer proof itself to consume it, 2026-08-23) | Explicit KMP Android target (confirmed blocked — see `docs/android/kmp-android-target-blocker.md` — needs a Kotlin/AGP combination that resolves it, not more repo-side workaround), iOS `LifecycleProvider` design (no contract exists for either platform yet, an open design question), physical-device proof for either platform (Android now has emulator proof, iOS only has Simulator proof — neither has run on actual hardware), a real Apple background-scheduler-triggered queue-drain tick (no `dataloom-scheduler-workmanager` Apple counterpart module exists yet — `#334` only proves the deterministic single-pass `queueWorker.run(...)` capability, mirroring `#325`'s own equivalent Android boundary); of the other built-in strategies, cache-first is now proven on both Android and iOS (only its PUSH-direction branch, on both platforms — see above; iOS's `#338` proof reached `Succeeded` first because `SqlDelightStorageProvider` already exposed a public outbound-seeding API that Android's `RoomStorageProvider` did not at the time; `#93` closed that storage-provider-level capability gap for Android, and `#342` updated the `#337` reference-consumer proof itself to consume it, so cache-first's PUSH branch is now fully symmetric end-to-end on both platforms); remote-first/hybrid still share the same `StrategyDurableQueueAdmitter` machinery but remain unexercised at this admission-then-replay layer on either platform (hybrid's own durable branches were investigated for this row and found to require `SERVE_LOCAL` or `StrategyReconciliationProvider` — capabilities the real `RoomStorageProvider` doesn't implement — so a hybrid proof needs either a fake/expanded storage provider or new coordinator support, not a same-shaped drop-in); network-only structurally never admits durable work, and adaptive only ever resolves into one of those other profiles rather than having its own branch; and retry/circuit-breaker/conflict-detection behavior during queue replay itself (each proven entry always succeeds on its first attempt) remains open, staged external consumers, and the complete parity matrix |
| 3 | [DL-040 retry and circuit breaker](https://github.com/dataloom-sdk/dataloom/issues/94) | QUALIFICATION BLOCKED | 72% | FR-RETRY-001–012 implementation mapping; built-in backoff/jitter, limits, hints, six timeout boundaries, durable Room/Apple circuit state, half-open leases, authorized administration, bounded telemetry, and common/platform-store recovery flows; real Android application-process kill/relaunch evidence for the circuit-breaker store — `AndroidProcessTerminationCircuitBreakerInstrumentedTest` kills and relaunches a genuine second `:circuitproof` Android process via `ActivityManager.killBackgroundProcesses` (this repository has no AndroidX Test Orchestrator or equivalent host-controlled runner) and proves the persisted phase, open deadline, consecutive-failure count, and probe generation survive that, closing the Android circuit-breaker slice of the previously-named "process termination/relaunch" gap; and now the same proof for the separate durable retry-budget structure — `AndroidProcessTerminationRetryBudgetInstrumentedTest` reuses the identical kill/poll/relaunch mechanics against a second, independent `:retrybudgetproof` process, driving the real `RoomQueueProvider` production coordinator's `enqueue`/`acquire`/`reschedule`/`defer` operations (never a test-only shortcut) against the `queue_entries` table's `retry_attempt_number`/`retry_window_started_at_ms`/`retry_last_evaluated_at_ms`/`retry_cumulative_delay_ms` columns — genuinely independent of the circuit-breaker store's own table — and proves the persisted retry attempt number, retry window start, last-evaluated instant, and cumulative delay survive a genuine process kill (`#327`, 2026-08-18), closing the Android retry-budget slice of the same gap; and now genuine cross-process contention for the single half-open probe permit itself — `AndroidCircuitBreakerProbeContentionInstrumentedTest` drives two genuinely different provider classes, `CircuitBreakerProbeContentionContentProviderA` and `CircuitBreakerProbeContentionContentProviderB` (both extending a shared `CircuitBreakerProbeContentionContentProviderBase` for their actual logic — real device CI first caught that declaring one class twice in the manifest compiles and packages cleanly but fails at runtime with `Unknown authority`, because Android's `PackageManagerService` supports only one live registration per `ComponentName`, package plus class name, regardless of how many manifest entries name it), declared once each in `src/androidTest/AndroidManifest.xml` under two different authorities/`android:process` values (`:circuitprobea`, `:circuitprobeb`) so Android hosts two genuinely independent OS processes, opens the circuit through one using a real wall-clock `DataLoomClock` (the two processes share no in-memory clock), then fires one `ContentResolver.call` at each from two threads released together by a `CyclicBarrier` so both real IPC calls launch as close to simultaneously as two real OS processes allow, and asserts exactly one of the two receives `CircuitBreakerPermission.ProbeAllowed` and the other `Rejected(PROBE_IN_FLIGHT)` — never both, never neither — mutual exclusion enforced by `RoomCircuitBreakerStateStore`'s real compare-and-set (`UPDATE ... WHERE record_version = :expectedVersion`, serialized across the two processes by Android's own SQLite file locking), not a test-only mutex, confirmed passing on a real Gradle Managed Device (`pixel2Api35`) in `android-validation.yml` CI (`#346`, 2026-08-24), closing the Android slice of the cross-process probe contention gap; and now the complete AC-FUNC-004 backoff/jitter/circuit-open/rejection/half-open-probe/recovery sequence proven through the real, composed provider flow itself — not the raw `CircuitBreakerExecutionGate`/`CircuitBreakerTransportOperationAdapter` reference flow the common/Android-Room/Apple-file qualification checkpoints already used, which each named this exact gap in their own "Remaining acceptance work" sections — `AndroidReferenceConsumerRetryCircuitQualificationInstrumentedTest` drives a real `DataLoomBuilder.providerProtectionConfiguration` wired to the real `RoomCircuitBreakerStateStore`, composed with `dataloom-android`'s real `androidDataLoomProviders`/`installAndroidProviders` wiring, through repeated real `DataLoom.protectedSynchronization.synchronize()` PULL calls against a fault-injecting transport — two failures durably open the circuit, a third attempt is rejected before the real transport is ever invoked, a second independently constructed Room connection to the same on-disk circuit database wins the half-open probe lease while a competing connection is rejected as `PROBE_IN_FLIGHT`, and normal operation recovers (first attempted as a Robolectric unit test but found to genuinely fail there — `RoomCircuitBreakerStateStore`'s `circuit_breaker_states` table does not work under Robolectric's SQLite shadow in this repository's current dependency versions, confirmed by reproducing the failure in isolation with no `DataLoomBuilder` involved, even though the same `DataLoomRoomDatabase`'s other tables do — so it now lives as `androidTest`, matching every other `RoomCircuitBreakerStateStore`-touching test in this repository); and `IosReferenceConsumerRetryCircuitQualificationTest` proves the identical sequence on Kotlin/Native iOS through the real `AppleFileCircuitBreakerStateStore` and `dataloom-platform-ios`'s real `appleDataLoomProviders`/`installAppleProviders` wiring (`#369`, 2026-08-25) | Prove real Apple process termination/relaunch (still fully open, for both circuit-breaker and retry-budget state; Apple also has no `android:process`-equivalent mechanism, so cross-process probe contention remains unprovable there for the same reason documented in `docs/apple/process-termination-investigation.md`), and the KMP Android third of the AC-FUNC-004 provider-flow gap specifically — native Android and KMP iOS are now proven (`#369`), but an explicit KMP-aware Android target remains confirmed blocked (`docs/android/kmp-android-target-blocker.md`), so no KMP-Android-specific provider-flow test can exist until that target itself is unblocked |
| 4 | [DL-041 conflict engine](https://github.com/dataloom-sdk/dataloom/issues/95) | IN PROGRESS | 57% | Custom detector/resolver contracts and orchestration foundations, a bounded first slice of durable unresolved-conflict persistence — `DurableUnresolvedConflictLog` (`#256`, 2026-08-11) — with a real caller, `DurableConflictDetectionCoordinator` (`#257`, 2026-08-11), now wired into `InboundPullSynchronizationPipeline` for real opt-in conflict detection during inbound pull (`#258`, 2026-08-11), reference-provider adoption of local-conflict-candidate reads — all four reference `StorageProvider`s (Room, SQLDelight, file-based, DataStore) now override `readLocalConflictCandidate` for real (`#309`–`#315`, 2026-08-16), conflict detection now genuinely circuit-breaker-protected through `ProviderProtectionStorageBridge`, closing the last named gap in that path (`#322`, 2026-08-17), and now the first deterministic built-in conflict-resolution strategy — `LastWriteWinsConflictResolver`, a deterministic remote-wins tiebreak honestly documented as a placeholder policy (no reliable recency evidence exists anywhere on `SynchronizationConflict`/`ChangeEvent` today; see `docs/api/conflict-resolution-strategies.md`) — wired as a genuinely selectable, genuinely invoked `ConflictResolver` through the existing `ConflictResolverRegistry`/`SynchronizationConflictOrchestrator` machinery with real decision application, plus a sixth durable-state domain adoption, `DurableResolvedConflictDecisionLog` (payload-free even for `Merge`, reusing `RoomDurableStateStore` with zero new Room code), with a real caller — `DurableConflictDetectionCoordinator` now optionally records `ConflictOrchestrationResult.Resolved` outcomes the same way it already recorded unresolved ones (`#328`, 2026-08-18), and now five additional deterministic built-in conflict-resolution policies alongside `LastWriteWinsConflictResolver` — `dataloom.builtin.client-wins` (explicit `UseLocal`), `dataloom.builtin.server-wins` (explicit `UseRemote`), `dataloom.builtin.timestamp` (a genuinely evidence-based recency resolver reading explicit, caller-supplied `dataloom.conflict.{local,remote}.updated-at-epoch-millis` metadata evidence, deferring rather than guessing when it is missing or malformed), `dataloom.builtin.reject` (`Fail`), and `dataloom.builtin.manual` (`Defer`) — all selected only by exact `ConflictResolverId` through the same `ConflictResolverRegistry`, with application overrides still taking precedence (`#329`, 2026-08-18), and now a genuine restart/concurrency proof for both durable conflict logs against the real `RoomDurableStateStore`-backed integration tests (previously only a single insert-then-load round trip; `DurableUnresolvedConflictLogTest`/`DurableResolvedConflictDecisionLogTest`'s own concurrency/idempotency coverage exercised only the abstract in-memory `DurableStateStore` fake, never the real Room-backed store class): a freshly reopened store instance recovers a previously committed record the way a process restart would, and a genuinely concurrent duplicate-record race — both racers' own reads observing no row yet, the loser's insert losing to the winner's, then reloading and re-evaluating per each log's own documented retry step — converges to `AlreadyRecorded` rather than losing the record or failing (`#345`, 2026-08-23), and now the resolved-decision loop is genuinely closed end to end: `InboundPullSynchronizationPipeline` previously detected, resolved, counted, and durably recorded a conflict decision but still always applied the original remote batch unchanged, so `UseLocal`/`Merge`/`Defer`/`Fail` had no real effect on application state — a gap this repository's own history never named explicitly until this PR's root-cause investigation surfaced it. A new internal `InboundConflictDecisionPreparer` stage now applies resolved decisions for real, fail-closed, opt-in via `resolvedConflictDecisionStore` (historical observational behavior is unchanged when it is absent): `UseRemote` maps to the remote event, `UseLocal` to omission, `Merge` to the validated merged event; `Defer`, `Fail`, unresolved, missing-detector, contradictory, and non-durable outcomes all block application and checkpoint advancement; a resolved decision may affect storage only once `Recorded`/`AlreadyRecorded` durable evidence exists; duplicate delivery or restart reconstructs the identical effective event and rejects a divergent record for the same `ConflictId`; detector inputs and merge entity identity are validated before durable recording so a bad detector implementation cannot poison the commit-once log; event order is preserved in mixed batches; and `inboundEventsApplied` now counts effective events actually passed to storage. Public ABI unchanged — the new preparation component is internal (`#333`, 2026-08-24), and now a policy-precedence investigation confirms entity type and workflow ID reliably reach a resolver's `resolve()` call today (tenant ID only when a host supplies it), but none of the three reach `ConflictResolverRegistry`'s exact-ID selection step — an explicit, repeatedly-documented invariant across `ConflictResolverRegistry`, `SynchronizationConflictOrchestrator`, and `DataLoomConflictDetectionSpec`, not a missing wire between an already-existing producer and consumer; even the narrowest entity-only precedence tier would reverse all three statements with no real caller to validate the new shape against, so no code changed (`docs/api/conflict-resolver-policy-precedence-investigation.md`, 2026-08-24), and now a bounded first slice of authorized manual conflict-resolution operations: `ConflictAdministrationCoordinator` applies an operator-supplied manual decision (`UseLocal`/`UseRemote`/`Merge`/`Fail`) to a specific already-durably-recorded unresolved conflict — a deliberately separate application path from `InboundConflictDecisionPreparer` (`#333`), which only ever applies a decision to the exact live `ChangeSet` batch that produced it, since by the time an operator decides an already-recorded conflict that batch has already failed closed with its checkpoint unadvanced — mirroring the `RetryAdministrationAuthorizer`/`DataLoomRetryAdministration` authorized-command pattern exactly: a host-supplied `ConflictAdministrationAuthorizer` (deny-by-default, no DataLoom-invented identity system), eligibility checked against `DurableUnresolvedConflictLog`/`DurableResolvedConflictDecisionLog` (nonexistent and already-resolved conflict IDs are well-defined `POLICY_REJECTED` outcomes, not exceptions), and actual application delegated to a host-owned `ConflictAdministrationExecutor` since neither durable log retains `ChangeEvent` payload content by design, making freshness/staleness checking against the live entity the host's responsibility rather than a problem this slice designs itself. A successful command is durably recorded by reusing the existing `ResolvedConflictDecisionRecord` with a sentinel `manual:<principalId>` resolver ID rather than a new durable-record type. Wired through `DataLoomBuilder.conflictAdministrationConfiguration(DataLoomConflictAdministrationSpec)`; omitting it leaves `DataLoom.conflictAdministration` `null` and every other capability byte-for-byte unchanged (`#367`, 2026-08-25) | Policy precedence (entity > workflow > tenant > global — investigated with no bounded first slice found yet; see `docs/api/conflict-resolver-policy-precedence-investigation.md`), loop/non-convergence quarantine, complete operational evidence and retry integration, production-store migration/process-death/high-contention qualification, one physical transaction spanning application storage, decision log, checkpoint, operational outbox, and audit evidence (today's guarantee is a durable commit barrier plus deterministic replay, not one atomic commit across all five), AC-FUNC-002, and full native Android/KMP Android/KMP iOS parity |
| 5 | [DL-042 events, observability, health, dashboard](https://github.com/dataloom-sdk/dataloom/issues/96) | IN PROGRESS | 49% | Canonical envelope/redaction contracts, deterministic V1 wire codec/upcast registry, in-process event dispatch, bounded retry/circuit metrics, logs, traces, exporter isolation, redacted health snapshot, the durable outbox's first real wired caller — every `SynchronizationEvent` (`Started`/`PhaseChanged`/`ProgressUpdated`/`RetryScheduled`/`ConflictDetected`/`Completed`) is bridged into an `OperationalEventEnvelope` by `SynchronizationOperationalEventBridge` and durably appended, opt-in via `DataLoomBuilder.operationalEventOutboxConfiguration(DataLoomOperationalEventOutboxSpec)`, with append/envelope-construction failures swallowed rather than surfaced as synchronization failures (`#324`, 2026-08-18) — a second real wired caller, bridging retry- and circuit-administration commands: every terminal `RetryAdministrationResult`/`CircuitAdministrationResult` returned by `DefaultDataLoomRetryAdministration`/`DefaultDataLoomCircuitAdministration` is bridged into an `OperationalEventEnvelope` by `RetryCircuitAdministrationOperationalEventBridge` and durably appended, opt-in via a second, separate spec — `DataLoomBuilder.retryCircuitAdministrationOperationalEventOutboxConfiguration(DataLoomRetryCircuitAdministrationOperationalEventOutboxSpec)` — reusing each command's own `commandId` for correlation and envelope identity rather than reading a clock or minting a new identifier, with the same swallowed-failure posture (`#335`, 2026-08-22) — and now a third real wired caller, bridging strategy-decision diagnostics: the same `StrategyDecisionEvent` `StrategySynchronizationExecutionCoordinator` already durably records via `DurableStrategyDecisionEventLog` (`#321`) is also bridged into an `OperationalEventEnvelope` by `StrategyDecisionOperationalEventBridge` and durably appended, opt-in via a third, separate spec — `DataLoomBuilder.strategyDecisionOperationalEventOutboxConfiguration(DataLoomStrategyDecisionOperationalEventOutboxSpec)`, which has no effect unless `strategyDiagnosticsConfiguration` is also configured — reusing the decision's own `StrategyDecisionId` for correlation and envelope identity, with the same swallowed-failure posture, genuinely additive over the per-decision diagnostic log alone since it feeds one ordered, cross-subsystem outbox stream a caller need not already know a `StrategyDecisionId` to read (`#336`, 2026-08-22) — and now a fourth real wired caller, bridging queue lifecycle: every already-persisted durable queue-entry state transition `DurableQueueExecutionProcessor` already computes and commits via the new optional `QueueEntryTransitionObserver` hook is bridged into an `OperationalEventEnvelope` by `QueueLifecycleOperationalEventBridge` and durably appended, opt-in via a fourth, separate spec — `DataLoomBuilder.queueLifecycleOperationalEventOutboxConfiguration(DataLoomQueueLifecycleOperationalEventOutboxSpec)`, which has no effect unless `queueWorkerConfiguration` is also configured — one envelope per witnessed transition, keyed by the already-unique `(QueueEntryId, QueueLeaseId)` pair rather than the entry alone (one entry legitimately passes through this bridge more than once over its lifetime), reusing `QueueEntry.synchronizationRequest.context`/`.workflowId` for correlation and envelope identity, with the same swallowed-failure posture (`#339`, 2026-08-23), and now extended to the circuit-aware queue worker too: `CircuitBreakerDurableQueueExecutionProcessor` accepts and notifies the exact same `QueueEntryTransitionObserver`, since per acquired entry it computes the identical `QueueEntryExecutionOutcome` the non-circuit-aware processor does and notifies immediately once the underlying `QueueProvider` transition call has already succeeded — independent of whether that entry's separate circuit-state recording (`QueueCircuitOperationRecord`) is later confirmed — so one spec now bridges both queue-worker paths (`#343`, 2026-08-23), and now a fifth real wired caller, bridging conflict resolution: every already-durably-recorded `UnresolvedConflictRecord`/`ResolvedConflictDecisionRecord` `DurableConflictDetectionCoordinator` already computes and commits via `DurableUnresolvedConflictLog`/`DurableResolvedConflictDecisionLog` (`#333`) is also bridged into an `OperationalEventEnvelope` by `ConflictResolutionOperationalEventBridge` and durably appended, opt-in via a fifth, separate spec — `DataLoomBuilder.conflictResolutionOperationalEventOutboxConfiguration(DataLoomConflictResolutionOperationalEventOutboxSpec)`, which has no effect unless `conflictDetectionConfiguration` is also configured — reusing the conflict's own `ConflictId` for correlation and (domain-prefixed `unresolved.`/`resolved.` so the two outcomes for the same conflict never collide) envelope identity, with the same swallowed-failure posture, genuinely additive over either per-conflict durable log alone since it feeds the same ordered, cross-subsystem outbox stream the other four bridges share (`#96`, 2026-08-24), and now a bounded first slice of durable-outbox retention: `DurableOperationalEventOutbox` takes an optional `maximumRetainedEntries: Int?` constructor parameter (`null` by default, byte-for-byte unchanged unbounded-accumulation behavior for every existing caller) that, when set, evicts the oldest entries as part of the same atomic compare-and-set write once an `append` would exceed the configured count, closing the previously-named "no retention or eviction policy" gap for the count-based case (`#344`, 2026-08-23), and now a bounded first slice of durable-outbox acknowledgement: `DurableOperationalEventOutbox.acknowledge(scope, id)` removes exactly one entry from a scope's retained list via the same atomic compare-and-set discipline `append` already uses, closing the previously-named "no acknowledgement or per-item removal" gap — deliberately scoped as operator-driven dismissal from a diagnostics view rather than work-queue completion, since every real bridge caller today durably appends a fire-and-forget side-record and never reads `entries` back to decide what to do next; acknowledging a nonexistent, already-acknowledged, or already-retention-evicted id is a well-defined no-op (`NotFound`) rather than a failure, and acknowledgement composes with `#344`'s retention with no special-case handling needed since both simply remove entries from the one shared persisted list (`#347`, 2026-08-24), and now a second, independent bounded retention mode closing the previously-named "age-based retention remains open" gap: `DurableOperationalEventOutbox` takes an optional `maximumRetainedAge: Duration?` constructor parameter paired with an optional `clock: DataLoomClock?` (both `null` by default, byte-for-byte unchanged behavior for every existing caller; setting `maximumRetainedAge` without `clock` is rejected at construction) that, when set, evicts every already-persisted entry (never the one an `append` call is itself adding) whose `OperationalEventEnvelope.occurredAt` is older than the configured age relative to `clock`'s current reading, reusing `occurredAt` rather than adding a new persisted "appended at" field so `OperationalEventOutboxState`'s shape stays unchanged; when both retention modes are configured, each `append` applies them in one fixed order as part of the same atomic write -- age-based eviction over the already-persisted entries, then the new entry is appended, then count-based eviction over the result -- so an entry evicted by either policy is evicted regardless of which one a caller might otherwise expect to run first (`#96`, 2026-08-24), and now a first real read-then-consume processing loop closing the previously-named gap: `DurableOperationalEventOutboxProcessor` (`io.dataloom.runtime.operational`) reads at most a caller-bounded `maxEntries` currently-retained entries for one scope (oldest first, exactly as `entries` already returns them), hands each to a caller-supplied `OperationalEventOutboxEntryHandler`, and acknowledges only the ones reporting `OperationalEventOutboxEntryOutcome.Processed`, leaving `Skipped`/`Failed` entries retained for a later pass — purely additive over `DurableOperationalEventOutbox`'s existing `entries`/`acknowledge` API (that class itself is unmodified, so all five existing bridges are unaffected), placed in `dataloom-runtime` rather than `dataloom-api` since a handler-driven processing cycle is a runtime implementation by that module's own rules, the same reasoning that already places `DurableQueueExecutionProcessor` there, and explicit about what concurrent `process` calls on the same scope do and do not guarantee: no entry is ever lost or double-acknowledged, inherited directly from `acknowledge`'s own compare-and-set retry loop, but the handler is not guaranteed exactly-once across racing calls on the same scope, so a caller with non-idempotent handler side effects must serialize `process` per scope itself (`#355`, 2026-08-24), and now bounded, opt-in filtering closing the previously-named "filtering" gap: `DurableOperationalEventOutboxProcessor.process` takes a new optional `filter: OperationalEventOutboxEntryFilter` parameter (a single-method `fun interface` — `matches(envelope): Boolean` — the same general-predicate shape `handler` itself already has, deliberately not a narrower structured type/category/source filter since no such structured filter-criteria type exists anywhere else in this codebase and a predicate is strictly more expressive), defaulting to a filter that accepts every entry so an unconfigured call reads and hands entries to `handler` exactly as before this parameter existed. Filtering is evaluated over every currently-retained entry for the scope before `maxEntries` is applied, so a rejected entry never counts against the bound — a caller filtering for a rare `OperationalEventType` is never starved by a batch full of common non-matching entries — and stays retained, untouched, for a later pass. `DurableOperationalEventOutbox` itself remains unmodified, deliberately: it already loads a scope's entire retained list as one persisted document per `entries` call, so filtering earlier inside that class would save no read work, only relocate the same in-memory `List.filter`, while that class's own documentation already scopes filtering out as separate follow-up work. New `OperationalEventOutboxProcessingSummary.filteredOut` counter (always `0` when unconfigured) keeps the summary honest about entries the filter excluded versus entries actually handed to the handler; when every retained entry is rejected, `process` returns `Processed` with an all-zero summary rather than `NoWork`, keeping "nothing retained at all" distinguishable from "nothing matched this cycle's filter". 4 new tests (configured filter processes only matching entries and leaves non-matching entries retained untouched; a rejected entry never counts against `maxEntries`; every entry filtered out returns `Processed` with an all-zero summary, not `NoWork`; unconfigured default-filter regression), purely additive ABI change (`#96`, 2026-08-25), and now a bounded first slice of the health/read-model gap: `dataLoomHealthSnapshot` (`io.dataloom.runtime.observation.health`, `dataloom-runtime`) is a pure, synchronous function that aggregates whichever already-queryable in-process subsystem state a caller supplies — `ProviderLifecycleCoordinator.state`, `BoundedRetryCircuitTelemetry.snapshot()`, and caller-awaited `DataLoomProvider.health()` results — into one redacted, point-in-time `DataLoomHealthSnapshot` value type; provider-health `DataLoomError` fields are redacted via the exact code/category/severity/recoverability-are-`PUBLIC`, message-is-`CONFIDENTIAL` (removed outright by the default policy), cause-never-included convention `SynchronizationOperationalEventBridge` already established, and `ProviderHealth.details`'s arbitrary metadata is deliberately reported only as a field count rather than field-by-field, since no existing conversion from free-form `DataLoomMetadata` keys into `ClassifiedData`'s bounded ASCII tokens exists to reuse; durable-outbox and queue-worker state are excluded from this slice since neither exposes a synchronous read path to aggregate today. 8 new tests, purely additive ABI change (JVM + iOS klib, verified via `checkKotlinAbi --rerun-tasks`). See `docs/api/health-snapshot.md` (`#96`, 2026-08-25) | Wire the outbox to every remaining subsystem's events (synchronization events, retry/circuit administration commands, strategy-decision diagnostics, both queue-worker paths' lifecycle, and now conflict resolution are all bridged — conflict resolution was the last subsystem this cell named explicitly, so every explicitly-named subsystem is now bridged; other, unnamed subsystems' events may still remain unbridged as they are identified going forward); add ordering guarantees beyond append order, replay, SDK-wide envelope adoption/instrumentation for the remaining unbridged subsystems, SDK-wide health aggregation across every subsystem (`dataLoomHealthSnapshot` covers only provider lifecycle state, retry/circuit telemetry, and caller-supplied provider health — durable-outbox and queue-worker state still have no synchronous read path to aggregate), and deployable operations dashboard/adaptor |
| 6 | [DL-043 asset synchronization](https://github.com/dataloom-sdk/dataloom/issues/97) | PARTIAL | 5% | A bounded first slice of `FR-ASSET-001`'s versioned asset manifest — `AssetManifest`, `AssetId`, `AssetMediaType`, `AssetChunkDescriptor`/`AssetChunkLayout`, `AssetCompressionMetadata`, `AssetEncryptionMetadata` — a pure, deeply immutable value type describing an already-decided asset shape (identity/version/size/media-type, whole-object and per-chunk checksums via `#234`'s `DataLoomDigest`, chunk geometry as an ordered contiguous-byte-range list rather than presupposing fixed-size chunking, and open-token compression/encryption algorithm labels deliberately deferring which algorithms V1 actually implements), with `AssetEncryptionMetadata` reusing `KeyReference` exactly as-is (never resolving it to key bytes) — see `docs/api/asset-manifest.md` (`#97`, 2026-08-24). Plus a bounded second slice: `DurableAssetManifestHistory` (`io.dataloom.api.asset`), durable, monotonic, bounded-retention history of an asset's applied `AssetManifest` revisions — the fifth real `DurableStateStore` domain adoption, reusing `RoomDurableStateStore` with zero new Room code, following gate `#93`'s own `DurableConfigurationHistory` adoption pattern — see `docs/api/durable-asset-manifest-history.md` (`#97`, 2026-08-24). No production wiring for either slice: no upload/download, chunking execution, streaming, durable resumable sessions, parallelism, quotas, cancellation, content-policy hooks, or a real caller of either the manifest type or its durable history | `FR-ASSET-002`–`012` in full: chunked upload/download, durable resumable sessions and restart recovery, bounded-memory streaming source/sink contracts, parallelism/fairness controls, an actual compression algorithm decision and implementation, an actual encryption algorithm decision and implementation, secure temporary-file creation/atomic promotion/cleanup, quota enforcement, cooperative cancellation, content allow/deny/scan/quarantine hooks, provider contracts, a provider test kit, and AC-FUNC-005 |
| 7 | [DL-044 plugin platform](https://github.com/dataloom-sdk/dataloom/issues/98) | NOT STARTED | 15% | Provider SPI exists; `dataloom-plugin-api`'s manifest/identifier/compatibility-range/dependency/execution-bounds/hook-point/lifecycle-label *contract shapes* also exist (`#93`'s own bounded slice, `#276`; see `docs/api/plugin-api.md`), but no V1 plugin lifecycle *behavior* is accepted — investigated in `docs/api/plugin-platform-first-slice-investigation.md`, which found no further bounded, dependency-free value-type slice remains to carve out | Implement deny-by-default registration/enablement, the lifecycle state machine and its transition enforcement, permission-grant enforcement with denied-operation diagnostics, execution-bounds enforcement, deterministic dependency ordering/cycle rejection, compatibility validation against a real running SDK version, hook-point callback dispatch, failure isolation/bulkheading, authorized hot disable, audit records, the certification kit, and a reference non-provider plugin |
| 8 | [DL-045 enterprise governance](https://github.com/dataloom-sdk/dataloom/issues/99) | NOT STARTED | 10% | Tenant identifiers and limited retry/circuit administration foundations exist — each verified directly against source and confirmed accurate, including real authorized/idempotent/audited evidence for retry and circuit administrative overrides specifically (`docs/status/dl-045-enterprise-governance-gap-analysis.md`'s full FR-ENT-001–012 cross-reference), not just this row's own prior description | Enforce tenant isolation, RBAC, signed policy packs, tamper-evident/offline audit, residency, support/fleet diagnostics, configuration locks, LTS/catalog governance, and AC-FUNC-010 (investigated and confirmed structurally blocked, not just undone: `#93`'s own shipped policy foundation already named signed policy packs "materially more speculative" and deferred them by name; a configuration-lock value type would protect a local-override mechanism with zero real producers today; an RBAC role/permission type needs a taxonomy decision this session cannot make unilaterally; see the gap-analysis doc's ordered checklist) |
| 9 | [DL-046 immutable V1 release](https://github.com/dataloom-sdk/dataloom/issues/100) | BLOCKED / NO-GO | 10% | Continuous JVM, Android, Apple, ABI, XCFramework, header, Swift-smoke, schema, and migration validation foundations exist — each verified directly against real, executing CI evidence (`docs/status/dl-046-release-readiness-checklist.md`), not just this row's own prior description, confirming the claim accurate as written while precisely scoping what each item does and does not cover (e.g. schema/migration drift-detection is Room-only; ABI checks are current-source-tree drift protection, not yet a frozen baseline against a released version, since no candidate has ever existed to freeze against) | Close #93–#99 and #101–#102 (zero of ten `#100` acceptance criteria can be marked complete today per the same checklist's full cross-reference — nine of ten are blocked on those gates or on the three human/business decisions below, not on engineering work); resolve the three human/business decisions `#100` itself owns (namespace/group-ID ownership and release authority for `io.dataloom`, exact V1 license text, signing-identity/key custody — see `docs/architecture/artifact-graph-bom-gap-analysis.md`); qualify one immutable candidate; verify staged consumers, compatibility, performance, security, SBOM/provenance/signatures/licenses (investigated and confirmed not independently bootstrappable today — an SBOM generated with no released candidate, group, or version behind it would be the SBOM equivalent of the "empty artifact wrapper" ADR-0002 already rejects), documentation, legal approval, publication, rollback, and post-publish smoke tests |

## Adoption readiness — reference providers

Not a frozen V1 gate. Tracks how easy DataLoom is to actually adopt today:
reference `StorageProvider`/`TransportProvider` implementations an application
can use directly or fork, so nobody has to hand-write both providers from
scratch before writing a line of sync code. Every technology below is an
equally-valid choice — none is preferred or required.

| Provider | Technology | Platforms | Status |
|---|---|---|---|
| Storage | Room | Android/JVM | ✅ Merged ([#209](https://github.com/dataloom-sdk/dataloom/issues/209) → [PR #212](https://github.com/dataloom-sdk/dataloom/pull/212)) |
| Storage | SQLDelight | Android + iOS | ✅ Merged ([#215](https://github.com/dataloom-sdk/dataloom/issues/215) → [PR #216](https://github.com/dataloom-sdk/dataloom/pull/216)) |
| Storage | Plain files (zero dependency) | Android + iOS | ✅ Merged ([#217](https://github.com/dataloom-sdk/dataloom/issues/217) → [PR #219](https://github.com/dataloom-sdk/dataloom/pull/219)) |
| Storage | DataStore (small key-value/cache-first data) | Android | ✅ Merged ([#218](https://github.com/dataloom-sdk/dataloom/issues/218) → [PR #220](https://github.com/dataloom-sdk/dataloom/pull/220)) |
| Transport | Ktor | Android + iOS + JVM | ✅ Merged ([#210](https://github.com/dataloom-sdk/dataloom/issues/210) → [PR #213](https://github.com/dataloom-sdk/dataloom/pull/213)) |
| Transport | Retrofit/OkHttp | Android/JVM | ✅ Merged ([#226](https://github.com/dataloom-sdk/dataloom/issues/226) → [PR #227](https://github.com/dataloom-sdk/dataloom/pull/227)) |
| Transport | GraphQL (Apollo Kotlin) | Android + iOS | ✅ Merged ([#222](https://github.com/dataloom-sdk/dataloom/issues/222) → [PR #224](https://github.com/dataloom-sdk/dataloom/pull/224)) |
| Transport | gRPC | Android/JVM (iOS not yet supported by the ecosystem) | ✅ Merged ([#223](https://github.com/dataloom-sdk/dataloom/issues/223) → [PR #225](https://github.com/dataloom-sdk/dataloom/pull/225)) |
| Docs | Getting-started quickstart | — | ✅ Merged ([#211](https://github.com/dataloom-sdk/dataloom/issues/211) → [PR #214](https://github.com/dataloom-sdk/dataloom/pull/214)) |

**9 of 9 merged.** SQLDelight's Android target hit a real AGP 9 + Kotlin Multiplatform plugin
incompatibility along the way — resolved by splitting the Android driver into its own
`dataloom-storage-sqldelight-android` module, matching this repo's established pattern for
Android-only adapters (see [Android overview](../android/README.md)).

## Market evidence gates

Engineering completion alone does not make DataLoom market-ready. The following
product evidence is required in addition to the V1 release gates.

| Market gate | Required target | Evidence accepted in repository | Remaining |
|---|---:|---:|---|
| Fully qualified built-in strategies | 6 | 0 of 6 | All six must pass their complete runtime, restart, failure, and platform matrices; planner-only or partial vertical slices do not count |
| Reference applications | 3 | 0 of 3 | Native Android, KMP Android, and KMP iOS reference consumers using staged/published-style artifacts |
| No-loss/no-duplication fault proof | Required | Partial | Complete crash, restart, duplicate, concurrency, cancellation, scheduler-failure, migration, and network-partition evidence across mandatory paths |
| Performance/resource benchmarks | Required | None accepted | Publish reproducible latency, throughput, memory, storage, battery/background, large-queue, and large-asset results with limits |
| Customer/problem interviews | 20 | 0 evidenced | Conduct and record twenty qualified interviews with findings and decision changes |
| Design partners | 5 | 0 evidenced | Secure five partners actively validating integration and product fit |
| Production pilots | 3 | 0 evidenced | Complete three monitored pilots with acceptance, reliability, and support evidence |
| Paid pilot | 1 | 0 evidenced | Convert at least one pilot to a paid engagement |
| Legal/publication approval | Required | Not accepted | Finalize license, namespace, signing/key custody, compliance evidence, support terms, and publication authority |

## Immediate execution order

Closed foundation gate #93 remains the single shared configuration, policy,
state, security, compatibility, and event boundary for every remaining engine.

1. Deliver one end-to-end strategy/platform vertical slice across native
   Android, KMP Android, and KMP iOS, then complete all six strategies through
   the same architecture (#102 + #101).
2. Add the host-controlled process lifecycle and cross-process harness needed
   to close the remaining retry/circuit acceptance criteria in #94.
3. Complete conflict, durable events/operations, assets, plugins, and enterprise
   governance in dependency order (#95–#99).
4. Build the three staged-artifact reference apps and publish the benchmark and
   fault-injection evidence.
5. Run customer validation alongside engineering: interviews, design partners,
   pilots, then one paid pilot.
6. Build and qualify one immutable V1 candidate and promote that exact artifact
   only after every engineering, security, legal, and market gate passes (#100).

Detailed evidence lives in the
[current DL-040 acceptance reconciliation](../audits/DL-040-current-acceptance-reconciliation.md)
and the
[V1 production-readiness audit](../audits/DL-AUDIT-004-v1-production-readiness.md).
