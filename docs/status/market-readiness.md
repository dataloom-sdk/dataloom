# Market-readiness dashboard (full detail)

This is the complete version of the dashboard summarized on the
[repository front page](../../README.md#market-readiness-dashboard). It
carries every dated entry and the full per-gate evidence text; the README
keeps only the current verdict and a short list of recent highlights so the
front page stays readable.

- **Last reconciled:** 2026-08-16
- **Recorded V1 target:** 2026-08-27
- **Current verdict:** **NO-GO — not production-ready or market-ready**
- **Accepted engineering/release gates:** **0 of 10** (`#93`–`#102` all open)

> [!NOTE]
> An earlier version of this dashboard reported `1 of 10` with `#93` marked
> complete. That was inaccurate: `#93` is still missing versioned
> configuration snapshots, a shared policy engine, SDK-wide adoption of the
> secure-random/integrity/HMAC primitives at real key/nonce/token/tamper-evidence
> sites, and an explicit KMP Android target, among other items below.
> Corrected here rather than left standing — see the acceptance rule this
> project holds itself to immediately below.

Progress below is acceptance-based, not a percentage of code written. A gate is
`COMPLETE` only when its issue criteria have executable evidence on the same
reviewed commit. `IN PROGRESS` therefore includes substantial implementations
that still have unqualified release behavior. A gate can absorb real, tested,
merged work for a long time before it flips to `COMPLETE` — that is by design,
not a sign nothing is happening. This log exists so that work is visible in
between gate flips.

The full V1 gate table below also carries an **Est. completion** column. It is
a rough, human-judged estimate of how much of each gate's required capability
set exists on `main` today — derived by weighing that row's own "Finished on
`main`" text against its "Still pending" text, banded to the nearest 5%. It
does **not** replace the `Status` column above it, is not derived from a
checkbox or code-coverage count (every acceptance-criteria checkbox on every
`#93`–`#100` issue is still literally unchecked in the GitHub issue body,
regardless of real progress — those checkboxes are not kept in sync), and a
gate can sit at a high estimate for a long time before its `Status` actually
flips to `COMPLETE`, same as everything else on this page. Treat it as a
progress signal, not an acceptance determination.

## Recently shipped (newest first)

| Date | What landed | Gate/track |
|---|---|---|
| 2026-08-17 | `MessageContentRedactor` (`#317`) — a defense-in-depth free-text redaction primitive (`io.dataloom.api.security`), complementary to the existing `DataLoomRedactor`/`ClassifiedData` structured-field redaction. Investigated the actual remaining gap first rather than assuming: the concrete, previously-cited violation (`ApolloErrorMapper` forwarding a wrapped exception's message after only truncating it) was already fixed by `#251`; what genuinely remained was structural — "no type-level guarantee against a future implementation" doing the same. Reference implementation `PatternBasedMessageContentRedactor` deterministically masks common secret-shaped patterns in free text (Bearer tokens, JWT-shaped tokens, AWS access keys, sensitive query-parameter values, URL Basic-Auth credentials, email addresses), explicitly documented as not a general-purpose scanner. Deliberately not wired into a real call site yet — no concrete consumer exists without inventing one, since the one known violation was already fixed by removing forwarding rather than needing redaction. 13 new tests, purely additive ABI change (pure common Kotlin, no platform split needed) | `#93` foundations |
| 2026-08-16 | `DataStoreStorageProvider` adopts `readLocalConflictCandidate` (`#315`) — the fourth and last reference `StorageProvider` to override this capability, **completing adoption across all four reference providers** (Room, SQLDelight, file-based, DataStore). Simplest of the four: `acknowledgeOutboundChanges` already deletes the event record for both `ACCEPTED` and `REJECTED` alike, so no separate accepted/rejected exclusion logic is needed; like file-based, has no per-entity index, so the same documented linear-scan cost applies. 5 new tests. Verified locally against an established pre-existing-failure baseline (this repo's own documented Windows-only `androidx.datastore` file-move limitation, 8/23 tests) rather than guessing at new failures — 2 of 5 new tests hit the identical known signature, confirmed by comparing JUnit XML failure messages side by side; real CI (Linux) confirmed all green | `#93`/`#95` foundations |
| 2026-08-16 | `FileStorageProvider` adopts `readLocalConflictCandidate` (`#313`) — the third reference `StorageProvider` to override this capability, after Room and SQLDelight. Same outbound-only principle, but with no per-entity index — `outbound.idx` is the only ordering this provider persists, so the implementation scans it in order and keeps the last match, a documented cost consistent with this provider's own reference/low-volume scope. `rejected/` is deliberately not consulted: a `REJECTED` event moves there with no ordering information relative to any other outbound entry, so there is no persisted evidence to say which is more recent — proven, not just asserted, via a dedicated test. Matches SQLDelight in deleting an `ACCEPTED` event rather than retaining it. 5 new tests against a real temp-directory filesystem | `#93`/`#95` foundations |
| 2026-08-16 | `SqlDelightStorageProvider` adopts `readLocalConflictCandidate` (`#311`) — the second reference `StorageProvider` to override this capability, after Room, same outbound-only principle. Real platform difference documented and tested rather than reconciled away: this provider's own `acknowledgeOutboundChanges` deletes the row for an `ACCEPTED` event instead of retaining it (Room retains it), so an entity whose only outbound edit was already accepted also correctly reports `NotFound` — a dedicated test proves this for real, not just the KDoc. New `selectLatestOutboundChangeForEntity` SQLDelight query. 4 new tests running against a real SQLite driver (JVM locally, cross-compiled for iOS) | `#93`/`#95` foundations |
| 2026-08-16 | `RoomStorageProvider` adopts `readLocalConflictCandidate` (`#309`) — the first reference `StorageProvider` to override this opt-in-since-`#258` capability. Deliberately considers only the *outbound* change-event log, never inbound: the outbound log is the local application's own record of pending/recent edits, which is exactly what a genuine local-vs-remote conflict compares against — an entity synced only via inbound apply correctly reports `NotFound`, since that's an ordinary remote update with no local edit to disagree with, not a conflict. New `OutboundChangeDao.findLatestEventForEntity` query returns the most recently appended outbound event per entity (change-set insertion order, then in-set event order — the schema has no shared wall-clock ordering across its outbound/inbound tables). 5 new tests (2 mocked-DAO unit, 3 against a real in-memory Room database, including one proving the outbound-only decision for real) | `#93`/`#95` foundations |
| 2026-08-16 | `AppleFileDurableStateStore` (`#307`) — the second real platform implementation of `DurableStateStore` (`#93` foundations), after Room. Reuses `AppleFileCircuitBreakerStateStore`'s proven `flock`/atomic-write discipline exactly; deliberately has no `namespace` concept, unlike `RoomDurableStateStore` — one dedicated file per instance, matching how Apple's other file-based providers (queue, circuit-breaker, retry-administration) already work. Fails closed identically to the Room implementation; `encode`/size checks run before the file lock is taken so a codec/size failure never touches the file. 12 new tests mirror `AppleFileCircuitBreakerStateStoreTest`'s coverage. Verified via cross-compile of production and test sources for all three Apple targets plus the real macOS `apple-validation.yml` CI leg (link + execute); no domain has adopted it yet — the three existing domain adoptions (configuration history, policy decisions, unresolved conflicts) remain Room-only | `#93` foundations |
| 2026-08-14 | Conflict detection wired into `DataLoomBuilder` for real (`#272`) — a foundational gap, not a hybrid-specific one: `InboundPullSynchronizationPipeline` has accepted an optional `InboundPullConflictDetectionConfiguration` since `#258`, and `StorageProvider.readLocalConflictCandidate` has existed since the same change, but nothing in `DataLoomBuilder` ever constructed one. Conflict detection was fully implemented and tested at the pipeline level, yet completely unreachable from any real application, for every direct pipeline and for all six built-in strategies (they share `buildStrategyPipelineRegistry()`). New `DataLoomConflictDetectionSpec` + `DataLoomBuilder.conflictDetectionConfiguration(spec)` close it — opt-in, byte-for-byte unchanged behavior when not called. Proven end-to-end through the public builder API, not just at the pipeline layer: configured detects a real conflict; unconfigured leaves `readLocalConflictCandidate` uncalled. 2 new tests, purely additive ABI change | `#93`/`#95`/`#102` foundations |
| 2026-08-14 | Hybrid's transport-free-PUSH gap closed with a new `AcceptedLocally` result type (`#271`) — `HybridSource.LOCAL` selected for a PUSH-direction request with no `SERVE_LOCAL` and no remote op previously had no result type that could represent it (`HYBRID_LOCAL_PUSH_NOT_YET_SUPPORTED`, a placeholder rejection); `HybridStrategyExecutor` now returns a genuinely transport-free `AcceptedLocally` terminal result — the first result type in the strategy engine that isn't tied to a transport or storage side-effect. The old enum entry is kept (ABI stability) but documented as no longer produced. `HybridStrategyExecutor` now handles every branch the evaluator can produce for `HybridStrategyProfile`. Purely additive ABI change | `#102` feature |
| 2026-08-14 | `dataloom-android` platform artifact (`#270`) — the second slice of `#101` (DL-039A), after the sibling-module-split pattern (proven precedent: `dataloom-storage-sqldelight-android`) was investigated and found not to transfer to `dataloom-model`/`dataloom-api`/`dataloom-core`/`dataloom-runtime` (zero Android-specific code in any of them; a same-shaped sibling would be a hollow, capability-free wrapper). A plain `com.android.library` (not KMP — an explicit KMP Android target remains confirmed blocked, see `docs/android/kmp-android-target-blocker.md`) aggregating the four core Android providers — connectivity, Room storage, Room queue, WorkManager scheduler — with `androidDataLoomProviders(context)`/`installAndroidProviders(...)` convenience wiring. Deliberately excludes DataStore/SQLDelight-android (opt-in individually) and any transport (DataLoom never bundles a default transport). `runtime-android-reference-consumer` refactored to dogfood it instead of hand-wiring the four providers directly. Also caught up `docs/architecture/modules.md` and `docs/android/README.md`, previously missing 5+ real modules | `#101` feature |
| 2026-08-14 | Native Android reference consumer (`#267`) — the first bounded slice of `#101` (DL-039A). New `runtime-android-reference-consumer` module wires `AndroidConnectivityProvider`, `RoomStorageProvider`, `RoomQueueProvider`, and `WorkManagerSchedulerProvider` into one real, buildable `DataLoom` instance through `DataLoomBuilder`'s public API — proving these four already-shipped provider modules actually compose together, which nothing in this repository exercised before. A compile-only fixture, mirroring `runtime-external-consumer`'s own already-established scope: proves compile-time composition, not runtime behavior (no Robolectric/instrumented test infrastructure exists yet — named as a follow-up, not silently claimed) or published-artifact resolution. Found and correctly did not fix a real, unrelated, pre-existing bug along the way: 8 of 23 `DataStoreStorageProviderTest` tests fail on a clean, unmodified `main` (confirmed via `git stash` before attributing it) — flagged as a separate task rather than scope-creeping this PR | `#101` feature |
| 2026-08-13 | Durable queue admission wired for cache-first, offline-first, and hybrid's shared default durable-work branch (`#265`) — the gap named across `#249`/`#259`/`#261` is closed for real, opt-in via a `QueuedSynchronizationWorkEncoder` (the same one `DataLoom.queueSubmission` already uses). New `StrategyDurableQueueAdmitter` bridges the existing, already-correct `StrategyQueueAdmissionEvaluator` (zero callers before this) into a real `QueueProvider.enqueue` call. Also fixes the coordinator's `DEFER`-disposition path, previously a bare in-memory signal regardless of what the plan required — this is offline-first's actual "no connectivity, durably admit local intent, survive process death" case. Offline-first's durable branch (and hybrid's PUSH-direction fallback) carries `ENQUEUE_DURABLE_WORK` alongside a full synchronous remote leg; running both risked a duplicate remote call once the durably admitted continuation is later processed by a queue worker, so admission replaces the synchronous attempt there (new `DurablyEnqueued` result) rather than running both. Cache-first/hybrid's `SERVE_LOCAL`-present branches admit alongside serving local state instead (`ServedFromCache.durableQueueEntryId`). A full-facade coordinator test (not just executor-level tests, which hand-build providers unconditionally) caught a real gap along the way: a plan's synchronous `requiredCapabilities` never include `TRANSPORT` for these durable branches, but the durable continuation does — provider resolution now widens to the continuation's requirements too, but only when an encoder is configured. Fully backward compatible: unconfigured callers see byte-for-byte identical behavior. 26 new tests, purely additive ABI change | `#102` feature |
| 2026-08-12 | Adaptive strategy execution verified end-to-end — no dedicated `AdaptiveStrategyExecutor` needed, by design (`#263`). `BuiltInSynchronizationStrategyEvaluator.evaluateAdaptive` resolves an `AdaptiveStrategyProfile` to one of its concrete candidates and delegates to that candidate's own evaluation branch, so it always reaches one of the five already-real strategy executors. Verifying this end-to-end (rather than assuming it, per this project's own discipline) surfaced a real, already-shipped bug: `RemoteFirstStrategyExecutor` and `HybridStrategyExecutor` both cast `request.profile` to their own concrete profile type unconditionally, which throws `ClassCastException` for any adaptive-resolved request (`request.profile` stays the outer adaptive profile, never replaced with the resolved candidate). Reproduced the real `ClassCastException` before fixing, per standing verify-before-fix discipline. Fixed with a new internal `resolvedProfile()` helper reading the resolved candidate via `effectiveProfileId` — purely internal, zero ABI impact. 5 new tests, full `jvmTest` and repo-wide `checkKotlinAbi` green | `#102` feature |
| 2026-08-12 | `HybridStrategyExecutor` — the fifth real strategy executor, after network-only, remote-first, cache-first, and offline-first (`#261`). The evaluator pre-selects `HybridSource.LOCAL`/`REMOTE` at evaluation time (not reactively, unlike remote-first's runtime fallback), so this executor just runs whichever source was selected, honoring `persistRemoteResult` via the same non-persisting transport-only path remote-first uses. Two branches are explicitly rejected rather than silently misexecuted: the reconciled-fallback branch (shares cache-first/offline-first's `DURABLE_REFRESH_NOT_YET_SUPPORTED` root cause) and a newly-discovered transport-free PUSH plan shape (`LOCAL` selected with no `SERVE_LOCAL` and no remote op) that no existing result type can represent — a new `HYBRID_LOCAL_PUSH_NOT_YET_SUPPORTED` reason instead of inventing a signature-incompatible success value. With all five concrete strategies now dispatched, the coordinator's `UNSUPPORTED_PLAN` fallback branch became unreachable via the public evaluator, so a test that depended on it was removed rather than repointed a third time. 13 new tests, purely additive ABI change (new class is `internal`) | `#102` feature |
| 2026-08-11 | `OfflineFirstStrategyExecutor` — the fourth real strategy executor, after network-only, remote-first, and cache-first (`#259`). Every plan this executor sees always carries a remote leg (the evaluator only reaches `EXECUTE` when connectivity is available); `SERVE_LOCAL` reuses the same `StrategyLocalFallbackProvider` pattern as cache-first, and `RECONCILE` (the profile default) is honored for real via `StrategyReconciliationProvider`, the first live-evaluation caller of that hook. The default durable-queue-admission path (`requireDurableQueue = true`) is explicitly rejected rather than silently misexecuted, reusing cache-first's `DURABLE_REFRESH_NOT_YET_SUPPORTED` reason for the same root-cause gap instead of duplicating it. 10 new tests, purely additive ABI change (new class is `internal`) | `#102` feature |
| 2026-08-11 | `DurableConflictDetectionCoordinator` wired into `InboundPullSynchronizationPipeline` for real, opt-in, best-effort conflict detection during inbound pull — the first `SynchronizationPipeline` to actually run conflict detection. Required a new opt-in `StorageProvider` capability (`readLocalConflictCandidate`, safe-defaulted so every existing provider keeps compiling unchanged) since no existing read gave the pipeline a local `ChangeEvent` to compare against; found and fixed a real decorator gap along the way (`TimeoutEnforcingStorageProvider` wasn't forwarding the new call) and named, rather than silently left, a second one (`ProviderProtectionStorageBridge`, needs a larger circuit-scope change). Detection is strictly observational — never blocks or alters `applyInboundChanges` (`#258`) | `#93`/`#95` foundations |
| 2026-08-11 | `DurableConflictDetectionCoordinator` — the first real runtime caller of a durable-state adapter shipped this cycle. Wraps `SynchronizationConflictOrchestrator.detectAndResolve` and durably records unresolved outcomes via `DurableUnresolvedConflictLog`, without reaching inside the orchestrator or changing its documented storage boundary; a durable-recording failure never hides the real orchestration result. Not yet called by any `SynchronizationPipeline` (`BidirectionalSynchronizationPipeline` never called the orchestrator at all before this) (`#257`) | `#93`/`#95` foundations |
| 2026-08-11 | Third real domain adoption of the durable-state contract — `DurableUnresolvedConflictLog`, a bounded first slice of ADR-0002's "durable unresolved-conflict" requirement: commit-once records of conflicts `SynchronizationConflictOrchestrator` could not auto-resolve (no resolver configured/found), payload-free by construction (never carries the original `ChangeEvent`). Reuses `RoomDurableStateStore` with zero new Room code — three domains in, the generic-store bet keeps paying off (`#256`) | `#93` foundations |
| 2026-08-10 | Second real domain adoption of the durable-state contract — `DurablePolicyDecisionLog` (commit-once durable record of `PolicyDecision`s, keyed by `PolicySetId`+`ExecutionId`; idempotent-retry vs. genuine-conflict distinguished, matching ADR-0002's "the resulting decision/evidence is committed with the state transition"), a reference codec (`PolicyDecisionRecordCodec`), and proof that `RoomDurableStateStore` genuinely generalizes — this domain reuses it directly with zero new Room DAO/entity code, just a codec and a `KeyEncoder` (`#255`) | `#93` foundations |
| 2026-08-10 | First real domain adoption of the durable-state contract — `DurableConfigurationHistory` (durable configuration snapshot history: `DataLoomConfigurationHistory`'s monotonic-version/bounded-retention/rollback semantics, now persisted via `DurableStateStore` with a bounded compare-and-set retry loop instead of held in a process-local field), a checksum-verified reference text codec (`ConfigurationHistoryStateCodec`), and `RoomDurableStateStore<TScope, TState>` — the first platform implementation of the generic contract, reusable by any future domain via injected `DurableStateScopeKeyEncoder`/`DurableStateCodec` rather than one-off persistence code per domain. Policy decisions remain the next unadopted domain (`#254`) | `#93` foundations |
| 2026-08-10 | Domain-agnostic durable-state contract — `DurableStateStore<TScope, TState>`, generalizing `CircuitBreakerStateStore`'s proven atomic load/compare-and-set pattern with schema-version/migration support (`DurableStateMigration`, `applyMigration`). Proven with a real in-memory implementation and 16 tests, not just a paper contract. No domain has adopted it as its real persistence path yet — that's the next step for configuration snapshots and policy decisions, both still in-memory-only (`#253`) | `#93` foundations |
| 2026-08-10 | Security-defaults audit: fixed a live default-`toString()` exposure gap on `DataLoomError.cause` (a plain `data class`'s auto-generated `toString()` renders a wrapped exception's raw message, which this codebase doesn't control) across all 32 confirmed implementations; documented the separate, still-open `message`-content redaction gap rather than silently leaving it unmentioned (`#251`) | `#93` foundations |
| 2026-08-10 | `CacheFirstStrategyExecutor` — the third real strategy executor, after network-only and remote-first (`#249`). Serve-from-cache and synchronous (non-durable) refresh work today; the default durable/scheduled-refresh path is explicitly rejected (`DURABLE_REFRESH_NOT_YET_SUPPORTED`) rather than silently misexecuted, pending separate queue-admission wiring. New public `ServedFromCache` result type, 10 new tests, purely additive ABI change | `#102` feature |
| 2026-08-10 | Direct test coverage for `StrategySynchronizationExecutionCoordinator`'s admission logic — PROVIDERS_NOT_INITIALIZED, STRATEGY_REJECTED, DEFER, INCOMPATIBLE_TRIGGER, INCOMPATIBLE_INPUT, UNSUPPORTED_PLAN, PROVIDER_RESOLUTION_FAILED, 7 tests (`#247`); closes out direct test coverage for the entire network-only/remote-first execution path (`#241`+`#243`+`#245`+`#247`, 34 tests total, zero production code changed) | `#102` hardening |
| 2026-08-10 | Direct unit test coverage for `RemoteFirstStrategyExecutor` — SERVE_LOCAL fast path, transport-only PULL, non-persisting BIDIRECTIONAL, and the provider-backed pipeline branch, 13 tests (`#245`); third hardening slice of the `#101`/`#102` pivot, closing a previously-undocumented zero-coverage gap on the second of only two strategies with real execution logic | `#102` hardening |
| 2026-08-10 | PUSH/BIDIRECTIONAL coordinator-level test coverage for network-only in `DataLoomBuilderProtectedStrategyTest` — the provider-protection/circuit-breaker layer previously only covered PULL (`#243`); second hardening slice of the `#101`/`#102` pivot | `#102` hardening |
| 2026-08-10 | Direct unit test coverage for `NetworkOnlyStrategyExecutor` — PUSH/PULL/BIDIRECTIONAL, all three acknowledgement-validation failure modes (`#241`); first hardening slice of the `#101`/`#102` pivot, closing a previously-undocumented zero-coverage gap on the network-only executor | `#102` hardening |
| 2026-08-09 | Deterministic policy foundation primitives — `PolicyEvaluator`, `PolicySet`, `PolicyCheckOutcome` (allow/deny/require-user-action/defer), time-bounded fail-closed evaluation (`#239`) | `#93` foundations |
| 2026-08-09 | Versioned configuration snapshot/precedence/rollback primitives — `DataLoomConfigurationResolver`, `ConfigurationSnapshot`, `DataLoomConfigurationHistory` (`#236`) | `#93` foundations |
| 2026-08-09 | Integrity and key-reference security primitives — `DataLoomDigestCalculator` (SHA-256/512), `DataLoomHmacCalculator` (HMAC, the "signature" stand-in), and `KeyReference` — for JVM, Android, and iOS (`#234`) | `#93` foundations |
| 2026-08-09 | Reference SQLDelight `StorageProvider` (Android + iOS); Android driver split into `dataloom-storage-sqldelight-android` to resolve an AGP 9 + Kotlin Multiplatform plugin incompatibility (`#216`) | Adoption readiness |
| 2026-08-09 | Reference Ktor `TransportProvider` (Android + iOS + JVM) (`#213`) | Adoption readiness |
| 2026-08-09 | Reference GraphQL `TransportProvider` (Apollo Kotlin, Android + iOS) (`#224`) | Adoption readiness |
| 2026-08-09 | Reference Room-backed `StorageProvider` for opaque outbound/inbound change sets and checkpoints (`#212`) | Adoption readiness |
| 2026-08-09 | Reference Retrofit/OkHttp `TransportProvider` (Android/JVM) (`#227`) | Adoption readiness |
| 2026-08-09 | Reference file-backed `StorageProvider` — zero database dependency, Android + iOS (`#219`) | Adoption readiness |
| 2026-08-09 | Reference gRPC-backed `TransportProvider` (grpc-kotlin, Android/JVM) (`#225`) | Adoption readiness |
| 2026-08-09 | `DataLoomSecureRandom` cryptographic randomness boundary for JVM/Android/iOS (`#230`) | `#93` foundations |
| 2026-08-09 | Corrected the dashboard's stale `1 of 10`/`#93`-complete claim to `0 of 10` | Documentation accuracy |
| 2026-08-08 | Production `DataLoomClock`/`DataLoomMonotonicClock` for JVM, Android, and iOS (`#221`) | `#93` foundations |
| 2026-08-08 | Reference DataStore-backed `StorageProvider` for small key-value/cache-first data (`#220`) | Adoption readiness |
| 2026-08-08 | Compile-verified getting-started quickstart guide (`#214`) | Adoption readiness |
| 2026-08-08 | 7 more reference-provider modules opened and in flight (Room, SQLDelight, file-based, Ktor, Retrofit, GraphQL, gRPC) | Adoption readiness |
| 2026-08-08 | Repository cleanup: 48 stale branches deleted, 10 superseded process issues closed | Repository hygiene |

## Full V1 gate table

| Next priority | V1 gate | Status | Est. completion | Finished on `main` | Still pending before the gate is complete |
|---:|---|---|---:|---|---|
| 0 | [DL-039 foundations, artifacts, compatibility](https://github.com/dataloom-sdk/dataloom/issues/93) | PARTIAL | 85% | Public API/provider/runtime boundaries, module rules, JVM and Kotlin/Native ABI baselines, external-consumer compilation, durable queue/retry/circuit state primitives, canonical operational envelope/redaction and deterministic bounded V1 wire codec/upcast registry, production wall-clock/monotonic-clock implementations for JVM, Android, and iOS (`#221`, 2026-08-08), a `DataLoomSecureRandom` cryptographic randomness boundary for JVM, Android, and iOS (`#230`, 2026-08-09), integrity/key-reference primitives — `DataLoomDigestCalculator`, `DataLoomHmacCalculator`, `KeyReference` — for JVM, Android, and iOS (`#234`, 2026-08-09), a bounded first slice of versioned configuration snapshots/precedence/rollback — `DataLoomConfigurationResolver`, `ConfigurationSnapshot`, `DataLoomConfigurationHistory` (`#236`, 2026-08-09), a bounded first slice of a deterministic policy foundation — `PolicyEvaluator`, `PolicySet`, `PolicyCheckOutcome` (`#239`, 2026-08-09), a security-defaults audit fixing default-`toString()` secret/credential exposure risk across all 32 `DataLoomError` implementations (`#251`, 2026-08-10), a domain-agnostic durable-state contract — `DurableStateStore<TScope, TState>`, generalizing `CircuitBreakerStateStore`'s proven atomic load/compare-and-set pattern with schema-version/migration support (`#253`, 2026-08-10), its first real domain adoption — `DurableConfigurationHistory` for configuration snapshot history, a checksum-verified reference codec, and a production `RoomDurableStateStore<TScope, TState>` platform implementation reusable by any future domain (`#254`, 2026-08-10), a second real domain adoption proving that reusability for real — `DurablePolicyDecisionLog` (commit-once durable `PolicyDecision` records, idempotent-retry vs. genuine-conflict distinguished) reusing `RoomDurableStateStore` with zero new Room code, just a codec and a `KeyEncoder` (`#255`, 2026-08-10), a third — `DurableUnresolvedConflictLog`, a bounded first slice of ADR-0002's "durable unresolved-conflict" requirement, payload-free by construction, again zero new Room code (`#256`, 2026-08-11), that log's first real caller — `DurableConflictDetectionCoordinator`, wrapping `SynchronizationConflictOrchestrator.detectAndResolve` to durably record unresolved outcomes without reaching inside the orchestrator or hiding real results behind durable-recording failures (`#257`, 2026-08-11), and that coordinator wired into `InboundPullSynchronizationPipeline` for real, opt-in conflict detection — the first `SynchronizationPipeline` to actually run it — via a new opt-in, safe-defaulted `StorageProvider.readLocalConflictCandidate` capability (`#258`, 2026-08-11), and that pipeline-level capability finally wired into the public facade — `DataLoomConflictDetectionSpec` + `DataLoomBuilder.conflictDetectionConfiguration(spec)`, closing a gap where conflict detection was fully implemented and tested but completely unreachable from any real application for every pipeline (`#272`, 2026-08-14), and a second real platform implementation of the durable-state contract — `AppleFileDurableStateStore`, reusing `AppleFileCircuitBreakerStateStore`'s proven `flock`/atomic-write discipline, one dedicated file per instance rather than a shared namespace (`#307`, 2026-08-16), and all four reference-provider adoptions of `readLocalConflictCandidate`, now complete — `RoomStorageProvider` (`#309`, 2026-08-16), `SqlDelightStorageProvider` (`#311`, 2026-08-16), `FileStorageProvider` (`#313`, 2026-08-16), and `DataStoreStorageProvider` (`#315`, 2026-08-16), all comparing only against outbound (local-edit) history, never inbound; SQLDelight's accept-deletes-the-row acknowledgement lifecycle, file-based's no-per-entity-index linear scan and `rejected/`-directory exclusion, and DataStore's delete-on-both-accepted-and-rejected lifecycle are each documented and tested as real, deliberate platform differences, not reconciled away, and a defense-in-depth free-text redaction primitive — `MessageContentRedactor`/`PatternBasedMessageContentRedactor`, masking common secret-shaped patterns in unstructured content such as `DataLoomError.message`, complementary to the existing structured-field `DataLoomRedactor` (`#317`, 2026-08-17) | Adopting the durable-state contract for events/assets/audit (the remaining out-of-scope domains) and for resolved `ConflictResolutionDecision`s (a separate, larger design question given `Merge`'s payload); closing the named `ProviderProtectionStorageBridge` gap where conflict detection is unavailable through circuit-breaker-protected storage (needs a breaking `StorageCircuitScopes` addition); wiring `DurableConfigurationHistory`/`DurablePolicyDecisionLog` into a real call site — genuinely blocked, not just undone, since neither `DataLoomConfigurationResolver.resolve` nor `PolicyEvaluator.evaluate` has a real caller anywhere in `dataloom-runtime` yet; any real domain adopting `AppleFileDurableStateStore` (the implementation exists and is verified, but every existing domain adoption remains Room-only); a real call site adopting `MessageContentRedactor` (the primitive exists and is tested, but nothing currently needs it — the one known violation was already fixed by removing unsafe forwarding rather than needing redaction); SDK-wide adoption of the secure-random/digest/HMAC/policy primitives at real call sites (including migrating `RetryPolicy`/`StrategyPolicy` onto the new policy foundation); an explicit KMP Android target (confirmed blocked, see `docs/android/kmp-android-target-blocker.md`); and the final published artifact graph/BOM (the graph itself is already fully specified in `ADR-0002`; building it as ~20 separate enforced modules is the remaining work) |
| 1 | [DL-039B six strategy engine](https://github.com/dataloom-sdk/dataloom/issues/102) | IN PROGRESS | 75% | Versioned contracts and deterministic planner for all six strategies; direct network-only, remote-first, cache-first (bounded first slice — serve-from-cache and synchronous refresh, `#249`, 2026-08-10), offline-first (`#259`, 2026-08-11), hybrid (`#261`, 2026-08-12), and adaptive (`#263`, 2026-08-12) — all six strategies now execute correctly, five with dedicated executors and adaptive verified end-to-end routing through them with no dedicated executor needed by design; that verification also caught and fixed a real `request.profile` unsafe-cast `ClassCastException` bug in the already-shipped `RemoteFirstStrategyExecutor` (would have hit hybrid too); durable queue admission now genuinely wired for cache-first/offline-first/hybrid's shared default durable-work branch and the coordinator's `DEFER` path, opt-in via a configured encoder (`#265`, 2026-08-13); hybrid's transport-free-PUSH branch now returns a genuine `AcceptedLocally` result instead of a placeholder rejection — `HybridStrategyExecutor` handles every branch the evaluator can produce (`#271`, 2026-08-14); conflict detection now reachable from `DataLoomBuilder` for all six strategies' shared inbound-pull leg, closing a gap where it was implemented and tested but unreachable from any real application (`#272`, 2026-08-14); Room v8/Apple v4 accepted-plan persistence; exact encoder/resolver correspondence; direct, protected, and queued replay without current-policy evaluation; typed fallback and reconciliation hooks, the latter now genuinely exercised outside the durable-continuation path by offline-first's `RECONCILE` branch | This gate's own acceptance criteria require native Android, KMP Android, and KMP iOS to expose equivalent observable decisions and recovery guarantees — blocked on `#101` platform parity, not on strategy-engine logic; durable strategy events/diagnostics; the full connectivity/cache/retry/conflict/cancellation/restart decision matrix audited per built-in profile |
| 2 | [DL-039A Android/KMP/iOS parity](https://github.com/dataloom-sdk/dataloom/issues/101) | IN PROGRESS | 65% | Native Android adapters, Kotlin/Native Apple targets, XCFramework assembly, Swift smoke compilation, a native Android reference consumer (`#267`) and an iOS reference consumer (`#299`) each proving their four core providers compose with `DataLoomBuilder`, real `dataloom-android` (`#270`) and `dataloom-platform-ios` (`#275`/`#292`/`#294`) platform aggregation artifacts, genuine runtime evidence on both platforms rather than compile-only proof — Robolectric (`#300`) and Kotlin/Native iOS Simulator (`#301`) `initialize()`/`shutdown()` proofs, a real `DataLoom.synchronize()` PULL pass on both platforms genuinely writing to storage (`#302`, `#303`) — and now a real Gradle Managed Device AVD emulator proof for Android (`#304`), one tier more real than Robolectric, reusing `dataloom-queue-room`'s/`dataloom-storage-room`'s existing managed-device CI mechanism | Explicit KMP Android target (confirmed blocked — see `docs/android/kmp-android-target-blocker.md` — needs a Kotlin/AGP combination that resolves it, not more repo-side workaround), iOS `LifecycleProvider` design (no contract exists for either platform yet, an open design question), physical-device proof for either platform (Android now has emulator proof, iOS only has Simulator proof — neither has run on actual hardware), queue admission/retry/circuit/conflict behavior during a real synchronization pass (today's proof is a direct PULL/FULL pass only), staged external consumers, and the complete parity matrix |
| 3 | [DL-040 retry and circuit breaker](https://github.com/dataloom-sdk/dataloom/issues/94) | QUALIFICATION BLOCKED | 70% | FR-RETRY-001–012 implementation mapping; built-in backoff/jitter, limits, hints, six timeout boundaries, durable Room/Apple circuit state, half-open leases, authorized administration, bounded telemetry, and common/platform-store recovery flows | Prove real Android and Apple process termination/relaunch, genuine cross-process probe contention where supported, and the complete AC-FUNC-004 provider flow through native Android, KMP Android, and KMP iOS |
| 4 | [DL-041 conflict engine](https://github.com/dataloom-sdk/dataloom/issues/95) | IN PROGRESS | 40% | Custom detector/resolver contracts and orchestration foundations, a bounded first slice of durable unresolved-conflict persistence — `DurableUnresolvedConflictLog` (`#256`, 2026-08-11) — with a real caller, `DurableConflictDetectionCoordinator` (`#257`, 2026-08-11), now wired into `InboundPullSynchronizationPipeline` for real opt-in conflict detection during inbound pull (`#258`, 2026-08-11), and reference-provider adoption of local-conflict-candidate reads — all four reference `StorageProvider`s (Room, SQLDelight, file-based, DataStore) now override `readLocalConflictCandidate` for real (`#309`–`#315`, 2026-08-16) | Ship deterministic built-in strategies, decision application, durable resolved-decision persistence, circuit-breaker-protected-storage conflict detection, audit, loop prevention/quarantine, precedence, restart/concurrency proof, and AC-FUNC-002 |
| 5 | [DL-042 events, observability, health, dashboard](https://github.com/dataloom-sdk/dataloom/issues/96) | IN PROGRESS | 40% | Canonical envelope/redaction contracts, deterministic V1 wire codec/upcast registry, in-process event dispatch, bounded retry/circuit metrics, logs, traces, exporter isolation, and redacted health snapshot | Add the durable outbox, ordering, acknowledgement/replay, retention, filtering, SDK-wide envelope adoption/instrumentation, health/read model, and deployable operations dashboard/adaptor |
| 6 | [DL-043 asset synchronization](https://github.com/dataloom-sdk/dataloom/issues/97) | NOT STARTED | 0% | No production asset subsystem accepted | Implement manifest, bounded streaming, upload/download chunking, durable resume, integrity, encryption metadata, quota, cancellation, secure temporary files, cleanup, content policy, and AC-FUNC-005 |
| 7 | [DL-044 plugin platform](https://github.com/dataloom-sdk/dataloom/issues/98) | NOT STARTED | 15% | Provider SPI exists; no V1 plugin lifecycle is accepted | Implement manifests, compatibility, deny-by-default permissions, lifecycle, resource bounds, deterministic ordering, isolation, hot disable, audit, certification kit, and a reference non-provider plugin |
| 8 | [DL-045 enterprise governance](https://github.com/dataloom-sdk/dataloom/issues/99) | NOT STARTED | 10% | Tenant identifiers and limited retry/circuit administration foundations exist | Enforce tenant isolation, RBAC, signed policy packs, tamper-evident/offline audit, residency, support/fleet diagnostics, configuration locks, LTS/catalog governance, and AC-FUNC-010 |
| 9 | [DL-046 immutable V1 release](https://github.com/dataloom-sdk/dataloom/issues/100) | BLOCKED / NO-GO | 10% | Continuous JVM, Android, Apple, ABI, XCFramework, header, Swift-smoke, schema, and migration validation foundations exist | Close #93–#99 and #101–#102; qualify one immutable candidate; verify staged consumers, compatibility, performance, security, SBOM/provenance/signatures/licenses, documentation, legal approval, publication, rollback, and post-publish smoke tests |

## Adoption readiness — reference providers

Not a frozen V1 gate. Tracks how easy DataLoom is to actually adopt today:
reference `StorageProvider`/`TransportProvider` implementations an application
can use directly or fork, so nobody has to hand-write both providers from
scratch before writing a line of sync code. Every technology below is an
equally-valid choice — none is preferred or required.

| Provider | Technology | Platforms | Status |
|---|---|---|---|
| Storage | Room | Android/JVM | ✅ Merged ([#209](https://github.com/dataloom-sdk/dataloom/issues/209) → [PR #212](https://github.com/dataloom-sdk/dataloom/pull/212)) |
| Storage | SQLDelight | Android + iOS | ✅ Merged ([#215](https://github.com/dataloom-sdk/dataloom/issues/215) → [PR #216](https://github.com/dataloom-sdk/dataloom/pull/216)) |
| Storage | Plain files (zero dependency) | Android + iOS | ✅ Merged ([#217](https://github.com/dataloom-sdk/dataloom/issues/217) → [PR #219](https://github.com/dataloom-sdk/dataloom/pull/219)) |
| Storage | DataStore (small key-value/cache-first data) | Android | ✅ Merged ([#218](https://github.com/dataloom-sdk/dataloom/issues/218) → [PR #220](https://github.com/dataloom-sdk/dataloom/pull/220)) |
| Transport | Ktor | Android + iOS + JVM | ✅ Merged ([#210](https://github.com/dataloom-sdk/dataloom/issues/210) → [PR #213](https://github.com/dataloom-sdk/dataloom/pull/213)) |
| Transport | Retrofit/OkHttp | Android/JVM | ✅ Merged ([#226](https://github.com/dataloom-sdk/dataloom/issues/226) → [PR #227](https://github.com/dataloom-sdk/dataloom/pull/227)) |
| Transport | GraphQL (Apollo Kotlin) | Android + iOS | ✅ Merged ([#222](https://github.com/dataloom-sdk/dataloom/issues/222) → [PR #224](https://github.com/dataloom-sdk/dataloom/pull/224)) |
| Transport | gRPC | Android/JVM (iOS not yet supported by the ecosystem) | ✅ Merged ([#223](https://github.com/dataloom-sdk/dataloom/issues/223) → [PR #225](https://github.com/dataloom-sdk/dataloom/pull/225)) |
| Docs | Getting-started quickstart | — | ✅ Merged ([#211](https://github.com/dataloom-sdk/dataloom/issues/211) → [PR #214](https://github.com/dataloom-sdk/dataloom/pull/214)) |

**9 of 9 merged.** SQLDelight's Android target hit a real AGP 9 + Kotlin Multiplatform plugin
incompatibility along the way — resolved by splitting the Android driver into its own
`dataloom-storage-sqldelight-android` module, matching this repo's established pattern for
Android-only adapters (see [Android overview](../android/README.md)).

## Market evidence gates

Engineering completion alone does not make DataLoom market-ready. The following
product evidence is required in addition to the V1 release gates.

| Market gate | Required target | Evidence accepted in repository | Remaining |
|---|---:|---:|---|
| Fully qualified built-in strategies | 6 | 0 of 6 | All six must pass their complete runtime, restart, failure, and platform matrices; planner-only or partial vertical slices do not count |
| Reference applications | 3 | 0 of 3 | Native Android, KMP Android, and KMP iOS reference consumers using staged/published-style artifacts |
| No-loss/no-duplication fault proof | Required | Partial | Complete crash, restart, duplicate, concurrency, cancellation, scheduler-failure, migration, and network-partition evidence across mandatory paths |
| Performance/resource benchmarks | Required | None accepted | Publish reproducible latency, throughput, memory, storage, battery/background, large-queue, and large-asset results with limits |
| Customer/problem interviews | 20 | 0 evidenced | Conduct and record twenty qualified interviews with findings and decision changes |
| Design partners | 5 | 0 evidenced | Secure five partners actively validating integration and product fit |
| Production pilots | 3 | 0 evidenced | Complete three monitored pilots with acceptance, reliability, and support evidence |
| Paid pilot | 1 | 0 evidenced | Convert at least one pilot to a paid engagement |
| Legal/publication approval | Required | Not accepted | Finalize license, namespace, signing/key custody, compliance evidence, support terms, and publication authority |

## Immediate execution order

Closed foundation gate #93 remains the single shared configuration, policy,
state, security, compatibility, and event boundary for every remaining engine.

1. Deliver one end-to-end strategy/platform vertical slice across native
   Android, KMP Android, and KMP iOS, then complete all six strategies through
   the same architecture (#102 + #101).
2. Add the host-controlled process lifecycle and cross-process harness needed
   to close the remaining retry/circuit acceptance criteria in #94.
3. Complete conflict, durable events/operations, assets, plugins, and enterprise
   governance in dependency order (#95–#99).
4. Build the three staged-artifact reference apps and publish the benchmark and
   fault-injection evidence.
5. Run customer validation alongside engineering: interviews, design partners,
   pilots, then one paid pilot.
6. Build and qualify one immutable V1 candidate and promote that exact artifact
   only after every engineering, security, legal, and market gate passes (#100).

Detailed evidence lives in the
[current DL-040 acceptance reconciliation](../audits/DL-040-current-acceptance-reconciliation.md)
and the
[V1 production-readiness audit](../audits/DL-AUDIT-004-v1-production-readiness.md).
